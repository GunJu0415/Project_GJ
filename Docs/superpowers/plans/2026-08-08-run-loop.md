# M1 런 루프 닫기 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 플레이어가 죽으면 런이 끝나고 허브로 돌아가며, 허브의 포탈로 새 런을 시작하는 로그라이트 루프를 완성한다.

**Architecture:** `UGJGameInstance`가 런 상태(`RebirthCount`)와 레벨 전환을 소유하고(레벨 전환에도 생존), `AGJGameMode`가 레벨 안에서의 사망 감지·연출·입력 전환을 맡는다. `AGJCharacter`는 "죽었다"고 알리기만 한다. 허브의 런 시작 포탈은 기존 `IGJInteractable` 상호작용 시스템을 그대로 재사용한다.

**Tech Stack:** UE 5.8, C++ (Enhanced Input, UMG `BindWidget` 패턴, `UGameplayStatics::OpenLevel`, ini `Config` 프로퍼티)

**설계 문서:** `Docs/superpowers/specs/2026-08-08-run-loop-design.md`

## Global Constraints

- **테스트 스위트가 없다.** 이 프로젝트의 검증은 **Live Coding 컴파일 통과 + 수동 PIE 확인**이다(설계 문서 7절). 각 태스크의 검증 단계는 자동화 테스트가 아니라 구체적인 PIE 조작 시나리오로 기술한다.
- **인코딩 주의 — 매우 중요.** `GJGameInstance.h`, `GJGameInstance.cpp`, `GJGameMode.cpp`의 기존 한글 주석은 **CP949(EUC-KR)**로 저장되어 있어 UTF-8 도구에서 깨져 보인다. 파일 자체는 정상이므로 **기존 주석 줄을 절대 다시 쓰거나 건드리지 말 것.** 반드시 부분 편집(추가/삽입)만 하고, 전체 덮어쓰기(Write)를 하지 말 것. 새로 만드는 파일은 UTF-8로 작성해도 된다.
- **새 UCLASS 파일은 라이브 코딩만으로 인식되지 않을 수 있다.** `UGJGameOverWidget`, `AGJRunPortal`은 컴파일 후에도 블루프린트 부모 클래스 목록에 안 뜨면 에디터 재시작이 필요하다.
- **레벨 이름은 전체 경로로 지정한다.** 예: `/Game/GJ/Level/TestLev`. 짧은 이름은 중복 시 잘못된 레벨이 열릴 수 있다.
- **커밋 메시지는 한국어**로 쓴다(기존 커밋 히스토리와 동일). 커밋 본문 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`를 넣는다.
- 새 C++ 파일은 `Source/Project_GJ/` 루트에 만든다(`Variant_Strategy/`, `Variant_TwinStick/`는 사용하지 않는 템플릿 잔재).

## 스펙에서 조정된 부분

설계 문서는 `EndRunAndReturnToHub()` 하나로 기술했으나, 구현 단계에서 **`EndRun()`과 `ReturnToHub()` 두 개로 분리**한다.

이유: 게임오버 위젯이 "방금 끝난 도전의 번호"를 표시하려면 **화면이 뜨는 시점에 이미 카운트가 올라가 있어야** 한다. 카운트 증가를 사망 순간(`OnPlayerDied`)에 두고 이동을 분리하면, 위젯 경로든 위젯 미할당 폴백 경로든 **카운트가 정확히 한 번만** 오른다. 설계 문서가 요구한 "증가 지점은 한 곳뿐" 원칙은 그대로 지켜진다.

## 파일 구조

| 파일 | 책임 | 태스크 |
|---|---|---|
| `Source/Project_GJ/GJGameInstance.h/.cpp` (수정) | 런 상태 소유, 레벨 전환 실행 | 2 |
| `Source/Project_GJ/GJGameMode.h/.cpp` (수정) | 사망 감지, 딜레이, 게임오버 화면, 입력 전환 | 2, 3 |
| `Source/Project_GJ/GJCharacter.cpp` (수정) | 사망 사실을 GameMode에 통보 | 2 |
| `Source/Project_GJ/GJGameOverWidget.h/.cpp` (신규) | 게임오버 화면 위젯 | 3 |
| `Source/Project_GJ/GJRunPortal.h/.cpp` (신규) | 허브의 런 시작 포탈 | 4 |
| `Config/DefaultGame.ini` (수정) | 허브·전투 레벨 경로 | 1 |
| `Config/DefaultEngine.ini` (수정) | 기본 맵 정리 | 1 |
| `Content/GJ/Level/HubLev.umap` (신규, 에디터) | 허브 레벨 | 1 |
| `Content/GJ/UI/WBP_GameOver.uasset` (신규, 에디터) | 게임오버 위젯 BP | 3 |
| `Content/GJ/BluePrint/BP_RunPortal.uasset` (신규, 에디터) | 포탈 BP | 4 |

---

## Task 1: 허브 레벨과 레벨 경로 설정

**Files:**
- Create: `Content/GJ/Level/HubLev.umap` (에디터 작업)
- Modify: `Config/DefaultGame.ini`
- Modify: `Config/DefaultEngine.ini:2-3`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces: 레벨 경로 문자열 두 개 — `/Game/GJ/Level/HubLev`(허브), `/Game/GJ/Level/TestLev`(전투). Task 2의 `UGJGameInstance` config 프로퍼티가 이 값을 읽는다.

- [ ] **Step 1: 허브 레벨 생성**

MCP가 연결되어 있으면 `editor_toolset.toolsets.scene.SceneTools` / `AssetTools`로 시도한다. `AssetTools.duplicate`로 기존 `TestLev`를 복제한 뒤 내용을 비우는 방법도 가능하다.

MCP로 안 되면 사용자에게 요청한다:
> 콘텐츠 브라우저에서 `Content/GJ/Level/` 폴더에 새 레벨을 `HubLev` 이름으로 만들어줘. Basic 템플릿이면 되고, `PlayerStart` 액터가 하나 있어야 해(없으면 배치해줘). 바닥(플레이어가 서 있을 지오메트리)도 하나 필요해.

허브에는 적이나 위험 요소를 두지 않는다(설계상 허브는 안전 지대라 사망 경로가 없다).

- [ ] **Step 2: 허브 레벨이 실제로 만들어졌는지 확인**

Run: `ls Content/GJ/Level/`
Expected: `HubLev.umap`과 `TestLev.umap`이 모두 보인다.

- [ ] **Step 3: `Config/DefaultGame.ini`에 레벨 경로 추가**

파일 맨 아래에 다음 섹션을 추가한다(기존 섹션은 건드리지 않는다):

```ini

[/Script/Project_GJ.GJGameInstance]
HubLevelName=/Game/GJ/Level/HubLev
CombatLevelName=/Game/GJ/Level/TestLev
```

- [ ] **Step 4: `Config/DefaultEngine.ini`의 기본 맵 수정**

2~3번 줄이 현재 Epic 템플릿 레벨을 가리키고 있다. 다음으로 바꾼다:

```ini
GameDefaultMap=/Game/GJ/Level/HubLev.HubLev
EditorStartupMap=/Game/GJ/Level/TestLev.TestLev
```

`GameDefaultMap`은 게임을 시작했을 때 열리는 맵이므로 허브가 맞다. `EditorStartupMap`은 개발 중 에디터를 켰을 때 열리는 맵이라 전투 레벨이 편하다.

- [ ] **Step 5: 설정이 제대로 들어갔는지 확인**

Run: `grep -n "GameDefaultMap\|EditorStartupMap" Config/DefaultEngine.ini && grep -A2 "GJGameInstance" Config/DefaultGame.ini`
Expected: `GameDefaultMap`이 `HubLev`, `EditorStartupMap`이 `TestLev`를 가리키고, `DefaultGame.ini`에 `HubLevelName`/`CombatLevelName` 두 줄이 보인다.

- [ ] **Step 6: 커밋**

```bash
git add Config/DefaultGame.ini Config/DefaultEngine.ini Content/GJ/Level/HubLev.umap
git commit -m "$(cat <<'EOF'
허브 레벨 추가 및 기본 맵 경로 정리

로그라이트 회차 구조의 거점이 될 HubLev를 만들고, 게임 시작 맵을
Epic 템플릿 레벨에서 허브로 변경. GJGameInstance가 읽을 레벨 경로를
DefaultGame.ini에 설정.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 사망하면 허브로 자동 이동 (루프 절반)

이 태스크가 끝나면 **위젯 없이도 루프의 절반이 실제로 돌아간다** — 죽으면 2초 뒤 허브로 이동하고 회차가 올라간다. 게임오버 화면은 Task 3에서 얹는다.

**Files:**
- Modify: `Source/Project_GJ/GJGameInstance.h:12-32`
- Modify: `Source/Project_GJ/GJGameInstance.cpp`
- Modify: `Source/Project_GJ/GJGameMode.h:12-20`
- Modify: `Source/Project_GJ/GJGameMode.cpp`
- Modify: `Source/Project_GJ/GJCharacter.cpp` (`HandleDeath` 함수)

**Interfaces:**
- Consumes: Task 1의 레벨 경로 (`DefaultGame.ini`의 `HubLevelName`/`CombatLevelName`)
- Produces:
  - `void UGJGameInstance::EndRun()` — `RebirthCount`를 1 증가
  - `void UGJGameInstance::ReturnToHub()` — 허브 레벨 열기
  - `void UGJGameInstance::StartNewRun()` — 전투 레벨 열기 (Task 4가 호출)
  - `int32 UGJGameInstance::GetRebirthCount() const` — 이미 존재 (Task 3이 호출)
  - `void AGJGameMode::OnPlayerDied()` — public
  - `void AGJGameMode::ShowGameOverScreen()` — protected (Task 3이 내용을 교체)

- [ ] **Step 1: `GJGameInstance.h`에 런 전환 API 추가**

⚠️ 기존 CP949 주석 줄은 손대지 말 것. `UCLASS()` 한 줄만 바꾸고 나머지는 삽입만 한다.

`UCLASS()` → `UCLASS(config=Game)`로 변경한다. 그리고 기존 `GetRebirthCount()` 선언 **아래**, `protected:` **위**에 다음을 삽입한다:

```cpp
	// 런 종료 - 회차 카운트만 올린다. 실제 이동은 ReturnToHub()가 담당.
	// 사망 시점에 딱 한 번 호출되므로, 게임오버 위젯을 거치든 안 거치든 카운트가 중복되지 않는다.
	UFUNCTION(BlueprintCallable, Category = "Run")
	void EndRun();

	// 허브 레벨로 이동
	UFUNCTION(BlueprintCallable, Category = "Run")
	void ReturnToHub();

	// 새 런 시작 - 전투 레벨로 이동 (카운트는 올리지 않음)
	UFUNCTION(BlueprintCallable, Category = "Run")
	void StartNewRun();
```

그리고 기존 `protected:` 블록의 `RebirthCount` 선언 **아래**에 다음을 삽입한다:

```cpp
	// 레벨 경로는 Config/DefaultGame.ini의 [/Script/Project_GJ.GJGameInstance] 섹션에서 지정한다.
	// Config 프로퍼티로 둔 덕분에 블루프린트 서브클래스를 따로 만들 필요가 없다.
	UPROPERTY(Config, EditDefaultsOnly, Category = "Run")
	FName HubLevelName;

	UPROPERTY(Config, EditDefaultsOnly, Category = "Run")
	FName CombatLevelName;
```

- [ ] **Step 2: `GJGameInstance.cpp`에 구현 추가**

⚠️ 기존 CP949 주석 줄은 손대지 말 것. 파일 맨 위 `#include` 블록에 아래 한 줄을 추가하고, 파일 맨 아래에 함수 세 개를 덧붙인다.

include 추가:
```cpp
#include "Kismet/GameplayStatics.h"
```

파일 끝에 추가:
```cpp
void UGJGameInstance::EndRun()
{
	// 회차를 올리는 곳은 여기 한 곳뿐이다. 사망 시점에 호출되므로,
	// 게임오버 위젯이 있든 없든(폴백 경로) 정확히 한 번만 증가한다.
	IncrementRebirthCount();
}

void UGJGameInstance::ReturnToHub()
{
	if (HubLevelName.IsNone())
	{
		// 잘못된 이름으로 OpenLevel을 호출하면 빈 맵에 갇혀버리므로, 아예 이동하지 않고 로그만 남긴다
		UE_LOG(LogTemp, Error, TEXT("ReturnToHub: HubLevelName is not set. Check [/Script/Project_GJ.GJGameInstance] in Config/DefaultGame.ini"));
		return;
	}

	UGameplayStatics::OpenLevel(this, HubLevelName);
}

void UGJGameInstance::StartNewRun()
{
	if (CombatLevelName.IsNone())
	{
		UE_LOG(LogTemp, Error, TEXT("StartNewRun: CombatLevelName is not set. Check [/Script/Project_GJ.GJGameInstance] in Config/DefaultGame.ini"));
		return;
	}

	UGameplayStatics::OpenLevel(this, CombatLevelName);
}
```

- [ ] **Step 3: `GJGameMode.h`에 사망 처리 인터페이스 추가**

기존 `AGJGameMode()` 선언 아래에 다음을 추가한다:

```cpp
	// 플레이어가 죽었을 때 캐릭터가 호출한다 - 런 종료 흐름을 시작한다
	void OnPlayerDied();

protected:
	// 사망부터 게임오버 화면까지의 딜레이(초)
	UPROPERTY(EditDefaultsOnly, Category = "Run")
	float DeathToGameOverDelay = 2.0f;

	// 딜레이가 끝났을 때 호출된다
	void ShowGameOverScreen();

	// 사망 통보가 여러 번 들어와도 한 번만 처리되게 하는 플래그
	bool bRunEnded = false;

	FTimerHandle GameOverTimerHandle;
```

- [ ] **Step 4: `GJGameMode.cpp`에 구현 추가**

⚠️ 기존 CP949 주석 줄과 생성자 내용은 손대지 말 것. include 두 줄을 추가하고 파일 끝에 함수 두 개를 덧붙인다.

include 추가:
```cpp
#include "GJGameInstance.h"
#include "TimerManager.h"
```

파일 끝에 추가:
```cpp
void AGJGameMode::OnPlayerDied()
{
	// 사망 통보가 중복으로 들어와도 타이머를 두 번 걸지 않도록 막는다
	if (bRunEnded)
	{
		return;
	}
	bRunEnded = true;

	// 런이 끝나는 시점은 "죽은 순간"이다. 화면이 뜨는 시점이 아니라 여기서 회차를 올려야
	// 게임오버 위젯이 방금 끝난 도전 번호를 그대로 표시할 수 있다.
	if (UGJGameInstance* GJGameInstance = Cast<UGJGameInstance>(GetGameInstance()))
	{
		GJGameInstance->EndRun();
	}

	GetWorldTimerManager().SetTimer(
		GameOverTimerHandle, this, &AGJGameMode::ShowGameOverScreen, DeathToGameOverDelay, false);
}

void AGJGameMode::ShowGameOverScreen()
{
	// Task 3에서 게임오버 위젯을 띄우는 코드로 교체된다.
	// 지금은 위젯 없이 곧바로 허브로 보내서 루프가 돌아가는지부터 확인한다.
	if (UGJGameInstance* GJGameInstance = Cast<UGJGameInstance>(GetGameInstance()))
	{
		GJGameInstance->ReturnToHub();
	}
}
```

- [ ] **Step 5: `GJCharacter.cpp`의 `HandleDeath`에서 GameMode에 통보**

⚠️ 기존 CP949 주석은 손대지 말 것.

include 블록에 추가:
```cpp
#include "GJGameMode.h"
```

`AGJCharacter::HandleDeath()`의 `bIsAutoFiring = false;` 아래에 추가:
```cpp

    // 런 종료 흐름(회차 카운트, 화면, 레벨 이동)은 전부 게임 모드가 담당한다.
    // 캐릭터는 "죽었다"는 사실만 알리고 게임 흐름은 알지 못한다.
    if (AGJGameMode* GJGameMode = Cast<AGJGameMode>(UGameplayStatics::GetGameMode(this)))
    {
        GJGameMode->OnPlayerDied();
    }
    else
    {
        // 다른 게임 모드를 쓰는 레벨에서 죽어도 크래시하지 않도록 로그만 남긴다
        UE_LOG(LogTemp, Warning, TEXT("HandleDeath: GameMode is not AGJGameMode. The run end flow will not run in this level."));
    }
```

- [ ] **Step 6: 컴파일**

사용자에게 요청한다:
> 에디터에서 **Ctrl+Alt+F11**로 라이브 코딩 컴파일해줘.

컴파일 후 `Saved/Logs/Project_GJ.log`에서 `LogLiveCoding`과 `error C`를 확인해 성공 여부를 판단한다.

Run: `grep -E "LogLiveCoding|error C" Saved/Logs/Project_GJ.log | tail -20`
Expected: `error C`가 없고 리로드 완료 로그가 보인다.

- [ ] **Step 7: PIE로 동작 확인**

사용자에게 요청한다:
> `TestLev`를 열고 PIE 실행해서, 적한테 맞아 죽어봐. 죽고 2초쯤 뒤에 허브 레벨(HubLev)로 넘어가면 성공이야.

확인 항목:
1. 죽은 뒤 약 2초 후 `HubLev`가 로드된다
2. 로그에 `Player Reborn. Current Rebirth Count: 1`이 찍힌다

Run: `grep -n "Current Rebirth Count" Saved/Logs/Project_GJ.log | tail -5`
Expected: 카운트 1이 기록되어 있다.

- [ ] **Step 8: 커밋**

```bash
git add Source/Project_GJ/GJGameInstance.h Source/Project_GJ/GJGameInstance.cpp Source/Project_GJ/GJGameMode.h Source/Project_GJ/GJGameMode.cpp Source/Project_GJ/GJCharacter.cpp
git commit -m "$(cat <<'EOF'
사망 시 런 종료 및 허브 이동 구현

GJGameInstance가 런 상태와 레벨 전환을 소유하고, GJGameMode가 사망 감지와
딜레이를 담당하는 구조. 캐릭터는 사망 사실만 통보한다.
회차 카운트는 사망 시점 한 곳에서만 증가하므로 이후 게임오버 위젯이
추가되어도 중복 증가하지 않는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 게임오버 화면

**Files:**
- Create: `Source/Project_GJ/GJGameOverWidget.h`
- Create: `Source/Project_GJ/GJGameOverWidget.cpp`
- Modify: `Source/Project_GJ/GJGameMode.h` (위젯 클래스 프로퍼티 추가)
- Modify: `Source/Project_GJ/GJGameMode.cpp` (`ShowGameOverScreen` 교체)
- Create: `Content/GJ/UI/WBP_GameOver.uasset` (에디터 작업)

**Interfaces:**
- Consumes:
  - `void UGJGameInstance::ReturnToHub()` (Task 2)
  - `int32 UGJGameInstance::GetRebirthCount() const` (기존 코드)
  - `void AGJGameMode::ShowGameOverScreen()` (Task 2에서 만든 것을 교체)
- Produces:
  - `class UGJGameOverWidget` — `BindWidget: ReturnToHubButton`(UButton), `BindWidgetOptional: RunCountText`(UTextBlock)
  - `void UGJGameOverWidget::SetRunCount(int32 RunCount)`

- [ ] **Step 1: `GJGameOverWidget.h` 생성**

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Blueprint/UserWidget.h"
#include "GJGameOverWidget.generated.h"

class UButton;
class UTextBlock;

// 런이 끝났을 때 뜨는 게임오버 화면. 기존 UGJPlayerHUDWidget/UGJInventoryWidget과 동일하게
// C++ 베이스 + BindWidget 패턴을 쓴다 - 디자이너에서는 이름이 맞는 위젯만 배치하면 되고
// 블루프린트 이벤트 그래프 작업이 필요 없다.
UCLASS()
class PROJECT_GJ_API UGJGameOverWidget : public UUserWidget
{
	GENERATED_BODY()

public:
	// 방금 끝난 도전의 번호를 표시한다 (게임 모드가 위젯을 띄운 직후 호출)
	UFUNCTION(BlueprintCallable, Category = "Run")
	void SetRunCount(int32 RunCount);

protected:
	virtual void NativeOnInitialized() override;

	// 디자이너에서 이 이름과 똑같은 Button을 추가해야 자동으로 바인딩됨
	UPROPERTY(meta = (BindWidget))
	UButton* ReturnToHubButton;

	// 선택 사항 - 몇 번째 도전이었는지 표시
	UPROPERTY(meta = (BindWidgetOptional))
	UTextBlock* RunCountText;

	UFUNCTION()
	void OnReturnToHubClicked();
};
```

- [ ] **Step 2: `GJGameOverWidget.cpp` 생성**

```cpp
#include "GJGameOverWidget.h"
#include "GJGameInstance.h"
#include "Components/Button.h"
#include "Components/TextBlock.h"

void UGJGameOverWidget::NativeOnInitialized()
{
	Super::NativeOnInitialized();

	if (ReturnToHubButton)
	{
		ReturnToHubButton->OnClicked.AddDynamic(this, &UGJGameOverWidget::OnReturnToHubClicked);
	}
}

void UGJGameOverWidget::SetRunCount(int32 RunCount)
{
	if (RunCountText)
	{
		RunCountText->SetText(FText::AsNumber(RunCount));
	}
}

void UGJGameOverWidget::OnReturnToHubClicked()
{
	// 회차 증가는 이미 사망 시점(AGJGameMode::OnPlayerDied)에서 끝났다.
	// 이 버튼은 이동만 담당한다.
	if (UGJGameInstance* GJGameInstance = Cast<UGJGameInstance>(GetGameInstance()))
	{
		GJGameInstance->ReturnToHub();
	}
}
```

- [ ] **Step 3: `GJGameMode.h`에 위젯 프로퍼티 추가**

파일 상단 `GJGameMode.generated.h` include 아래에 전방 선언을 추가한다:

```cpp
class UGJGameOverWidget;
```

그리고 Task 2에서 추가한 `DeathToGameOverDelay` 선언 **아래**에 다음을 삽입한다:

```cpp
	// 게임오버 화면 위젯 클래스. BP_GJGameMode 디테일 패널에서 WBP_GameOver를 할당한다.
	// 비어 있으면 위젯 없이 곧바로 허브로 이동한다(에셋을 아직 안 만들었어도 루프가 멈추지 않도록)
	UPROPERTY(EditDefaultsOnly, Category = "Run")
	TSubclassOf<UGJGameOverWidget> GameOverWidgetClass;

	UPROPERTY()
	UGJGameOverWidget* GameOverWidgetInstance;
```

- [ ] **Step 4: `GJGameMode.cpp`의 `ShowGameOverScreen` 교체**

include 추가:
```cpp
#include "GJGameOverWidget.h"
#include "Kismet/GameplayStatics.h"
#include "GameFramework/PlayerController.h"
```

Task 2에서 만든 `ShowGameOverScreen()` 함수 전체를 다음으로 교체한다:

```cpp
void AGJGameMode::ShowGameOverScreen()
{
	// 위젯 클래스가 아직 할당되지 않았어도 플레이어가 갇히지 않도록, 위젯 없이 곧바로 허브로 보낸다
	if (!GameOverWidgetClass)
	{
		UE_LOG(LogTemp, Warning, TEXT("ShowGameOverScreen: GameOverWidgetClass is not set on BP_GJGameMode. Skipping the game over screen."));
		if (UGJGameInstance* GJGameInstance = Cast<UGJGameInstance>(GetGameInstance()))
		{
			GJGameInstance->ReturnToHub();
		}
		return;
	}

	APlayerController* PC = UGameplayStatics::GetPlayerController(this, 0);
	if (!PC)
	{
		return;
	}

	GameOverWidgetInstance = CreateWidget<UGJGameOverWidget>(PC, GameOverWidgetClass);
	if (!GameOverWidgetInstance)
	{
		return;
	}

	GameOverWidgetInstance->AddToViewport();

	// 회차는 사망 시점에 이미 올라가 있으므로, 지금 값이 곧 방금 끝난 도전의 번호다
	if (UGJGameInstance* GJGameInstance = Cast<UGJGameInstance>(GetGameInstance()))
	{
		GameOverWidgetInstance->SetRunCount(GJGameInstance->GetRebirthCount());
	}

	// 게임을 일시정지하지 않고 입력만 UI로 넘긴다. 죽은 뒤에도 적이 계속 움직이지만
	// 플레이어의 캡슐/메시 콜리전이 이미 해제되어 있어 무해하고,
	// 일시정지 + 입력 모드 조합에서 겪었던 포커스 문제를 피할 수 있다.
	FInputModeUIOnly InputMode;
	InputMode.SetWidgetToFocus(GameOverWidgetInstance->TakeWidget());
	PC->SetInputMode(InputMode);
	PC->bShowMouseCursor = true;
}
```

- [ ] **Step 5: 컴파일**

사용자에게 요청한다:
> **Ctrl+Alt+F11**로 컴파일해줘. `UGJGameOverWidget`은 새로 만든 클래스라, 컴파일 뒤에 위젯 블루프린트 부모 목록에 안 보이면 에디터를 한 번 재시작해야 할 수도 있어.

Run: `grep -E "LogLiveCoding|error C" Saved/Logs/Project_GJ.log | tail -20`
Expected: `error C`가 없다.

- [ ] **Step 6: `WBP_GameOver` 위젯 블루프린트 생성**

MCP가 연결되어 있으면 `UMGToolSet`으로 만든다:
1. `CreateWidgetBlueprint(folderPath: "/Game/GJ/UI", assetName: "WBP_GameOver", parentClass: "/Script/Project_GJ.GJGameOverWidget")`
2. 루트에 `CanvasPanel` 추가
3. `VerticalBox`를 화면 중앙에 추가
4. `TextBlock`을 추가하고 이름을 **`RunCountText`**로 지정
5. `Button`을 추가하고 이름을 **`ReturnToHubButton`**로 지정 (이름이 정확히 일치해야 `BindWidget`이 연결된다)
6. 버튼 안에 `TextBlock`을 넣고 텍스트를 "허브로"로 설정
7. `CompileWidgetBlueprint` 후 `AssetTools.save_assets`

⚠️ MCP로 위젯 블루프린트 클래스를 참조할 때는 에셋 경로가 아니라 `_C` 서픽스가 붙은 생성 클래스 경로를 써야 한다(예: `/Game/GJ/UI/WBP_GameOver.WBP_GameOver_C`).

MCP로 안 되면 사용자에게 요청한다:
> `Content/GJ/UI/`에 `UGJGameOverWidget`을 부모로 하는 위젯 블루프린트를 `WBP_GameOver` 이름으로 만들어줘. 안에 Button 하나를 넣고 이름을 정확히 `ReturnToHubButton`으로, TextBlock 하나를 `RunCountText`로 지정해줘.

- [ ] **Step 7: `BP_GJGameMode`에 위젯 클래스 할당**

MCP로 시도한다:
`ObjectTools.set_properties`로 `BP_GJGameMode`의 CDO에 `GameOverWidgetClass = /Game/GJ/UI/WBP_GameOver.WBP_GameOver_C`를 설정한다. 먼저 `ObjectTools.list_properties`로 정확한 프로퍼티 이름을 확인한다.

MCP로 안 되면 사용자에게 요청한다:
> `BP_GJGameMode`를 열고 디테일 패널의 Run 카테고리에서 `Game Over Widget Class`를 `WBP_GameOver`로 지정해줘.

- [ ] **Step 8: PIE로 동작 확인**

사용자에게 요청한다:
> `TestLev`에서 PIE 실행하고 죽어봐.

확인 항목:
1. 죽고 2초 뒤 게임오버 화면이 뜬다
2. 마우스 커서가 보이고 버튼을 클릭할 수 있다
3. `RunCountText`에 숫자가 표시된다
4. "허브로" 버튼을 누르면 `HubLev`가 로드된다

- [ ] **Step 9: 커밋**

```bash
git add Source/Project_GJ/GJGameOverWidget.h Source/Project_GJ/GJGameOverWidget.cpp Source/Project_GJ/GJGameMode.h Source/Project_GJ/GJGameMode.cpp Content/GJ/UI/WBP_GameOver.uasset Content/GJ/BluePrint/BP_GJGameMode.uasset
git commit -m "$(cat <<'EOF'
게임오버 화면 추가

사망 후 딜레이가 끝나면 게임오버 위젯을 띄우고 입력을 UI로 전환한다.
위젯 클래스가 할당되지 않았으면 화면 없이 곧바로 허브로 보내서
플레이어가 갇히지 않도록 했다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 런 시작 포탈 (루프 완성)

**Files:**
- Create: `Source/Project_GJ/GJRunPortal.h`
- Create: `Source/Project_GJ/GJRunPortal.cpp`
- Create: `Content/GJ/BluePrint/BP_RunPortal.uasset` (에디터 작업)
- Modify: `Content/GJ/Level/HubLev.umap` (포탈 배치)

**Interfaces:**
- Consumes:
  - `void UGJGameInstance::StartNewRun()` (Task 2)
  - `IGJInteractable::Interact(AGJCharacter* Interactor)` (기존 인터페이스)
- Produces: `class AGJRunPortal` — 허브에 배치하는 상호작용 액터

- [ ] **Step 1: `GJRunPortal.h` 생성**

```cpp
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "GJInteractable.h"
#include "GJRunPortal.generated.h"

class UStaticMeshComponent;
class USphereComponent;
class AGJCharacter;

// 허브에 놓는 "런 시작" 포탈. 아이템(AGJItemBase)/무기(AGJWeaponBase)와 똑같은
// IGJInteractable 방식을 쓰므로 플레이어 쪽 입력 코드는 손댈 필요가 없다.
UCLASS()
class PROJECT_GJ_API AGJRunPortal : public AActor, public IGJInteractable
{
	GENERATED_BODY()

public:
	AGJRunPortal();

	// IGJInteractable - 상호작용 범위 안에서 상호작용 입력을 받으면 새 런을 시작한다
	virtual void Interact_Implementation(AGJCharacter* Interactor) override;

protected:
	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Components")
	USceneComponent* RootComp;

	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Components")
	UStaticMeshComponent* PortalMesh;

	// 상호작용 판정 범위 - AGJItemBase의 InteractionCollision과 동일한 "Trigger" 프로필.
	// 아이템보다 넉넉하게 잡아서 포탈 근처에 서기만 해도 반응하게 한다.
	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Components")
	USphereComponent* InteractionCollision;
};
```

- [ ] **Step 2: `GJRunPortal.cpp` 생성**

```cpp
#include "GJRunPortal.h"
#include "Components/StaticMeshComponent.h"
#include "Components/SphereComponent.h"
#include "GJCharacter.h"
#include "GJGameInstance.h"

AGJRunPortal::AGJRunPortal()
{
	PrimaryActorTick.bCanEverTick = false;

	RootComp = CreateDefaultSubobject<USceneComponent>(TEXT("RootComp"));
	SetRootComponent(RootComp);

	PortalMesh = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("PortalMesh"));
	PortalMesh->SetupAttachment(RootComponent);
	// 메시는 비주얼 전용 - 플레이어가 포탈을 통과해 걸어다녀도 막히지 않게 한다
	PortalMesh->SetCollisionProfileName(TEXT("NoCollision"));

	InteractionCollision = CreateDefaultSubobject<USphereComponent>(TEXT("InteractionCollision"));
	InteractionCollision->SetupAttachment(RootComponent);
	InteractionCollision->InitSphereRadius(200.f);
	InteractionCollision->SetCollisionProfileName(TEXT("Trigger"));
}

void AGJRunPortal::Interact_Implementation(AGJCharacter* Interactor)
{
	// "지금 정말 범위 안인지"는 각 구현부가 스스로 판단한다 (IGJInteractable의 규칙)
	if (!Interactor || !InteractionCollision->IsOverlappingActor(Interactor))
	{
		return;
	}

	if (UGJGameInstance* GJGameInstance = Cast<UGJGameInstance>(GetGameInstance()))
	{
		GJGameInstance->StartNewRun();
	}
}
```

- [ ] **Step 3: 컴파일**

사용자에게 요청한다:
> **Ctrl+Alt+F11**로 컴파일해줘. `AGJRunPortal`도 새 클래스라 블루프린트 부모 목록에 안 보이면 에디터 재시작이 필요할 수 있어.

Run: `grep -E "LogLiveCoding|error C" Saved/Logs/Project_GJ.log | tail -20`
Expected: `error C`가 없다.

- [ ] **Step 4: `BP_RunPortal` 생성 및 허브에 배치**

MCP로 시도한다:
1. `BlueprintTools.create`로 `AGJRunPortal`을 부모로 하는 `BP_RunPortal`을 `/Game/GJ/BluePrint`에 생성
2. `ObjectTools.set_properties`로 `PortalMesh`에 스태틱 메시를 지정 (예: `/Engine/BasicShapes/Cylinder.Cylinder` — 눈에 보이기만 하면 되므로 임시로 충분하다)
3. `HubLev`를 연 뒤 `SceneTools`로 `BP_RunPortal`을 `PlayerStart` 근처(약 300유닛 앞)에 배치
4. 레벨 저장

MCP로 안 되면 사용자에게 요청한다:
> `AGJRunPortal`을 부모로 하는 블루프린트를 `Content/GJ/BluePrint/`에 `BP_RunPortal` 이름으로 만들고, PortalMesh에 아무 스태틱 메시(원통 같은 거)를 넣어줘. 그리고 `HubLev`를 열어서 PlayerStart 근처에 하나 배치하고 레벨 저장해줘.

- [ ] **Step 5: 루프 전체를 반복 검증**

사용자에게 요청한다:
> `HubLev`를 열고 PIE 실행해서 아래 순서로 두 바퀴 돌려봐.

확인 항목:
1. 허브에서 포탈에 다가가 상호작용 키(E)를 누르면 `TestLev`가 로드된다
2. 전투 레벨에서 죽으면 2초 뒤 게임오버 화면이 뜬다
3. "허브로"를 누르면 `HubLev`로 돌아온다
4. **한 바퀴 더 반복**했을 때도 똑같이 동작한다
5. 로그에서 `Current Rebirth Count`가 1 → 2로 증가한다
6. 두 번째 런을 시작했을 때 **HP/MP가 가득 차 있고, 인벤토리가 비어 있으며, 시작 무기만 들려 있다** (레벨 재로드로 자동 초기화된다는 설계 가정 검증)

Run: `grep -n "Current Rebirth Count" Saved/Logs/Project_GJ.log | tail -5`
Expected: 카운트가 1, 2로 순차 증가한 기록이 보인다.

6번이 실패하면(뭔가 이전 런 상태가 남아 있으면) 설계 가정이 틀린 것이므로, 원인을 파악해 보고한 뒤 다음 단계로 넘어가지 않는다.

- [ ] **Step 6: 커밋**

```bash
git add Source/Project_GJ/GJRunPortal.h Source/Project_GJ/GJRunPortal.cpp Content/GJ/BluePrint/BP_RunPortal.uasset Content/GJ/Level/HubLev.umap
git commit -m "$(cat <<'EOF'
허브에 런 시작 포탈 추가로 로그라이트 루프 완성

기존 IGJInteractable 상호작용 시스템을 그대로 재사용해서, 허브 포탈에
상호작용하면 전투 레벨로 이동한다. 이로써 시작-사망-복귀-재시작 루프가
닫혔다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 개발 가이드 갱신

**Files:**
- Modify: `Docs/DevGuide.md`
- Modify: `DevGuide.html`

**Interfaces:**
- Consumes: Task 1~4에서 만든 모든 클래스와 흐름
- Produces: 없음 (문서화)

- [ ] **Step 1: `Docs/DevGuide.md`에 런 루프 섹션 추가**

문서 최상단의 "마지막 갱신 시점"을 `2026-08-08 세션 기준 (M1 런 루프까지 반영됨)`으로 바꾼다.

1절 "클래스 계층 한눈에 보기"의 UI 목록에 다음 줄을 추가한다:
```
├─ UGJGameOverWidget      — 런 종료 시 뜨는 게임오버 화면
```
그리고 최상위 목록에 다음을 추가한다:
```
AGJRunPortal (AActor, IGJInteractable 구현) — 허브의 런 시작 포탈
```

새 절을 추가한다(6절 적 AI와 7절 UI 사이):

```markdown
## 6.5 런 루프 (로그라이트 회차 구조)

플레이어가 죽으면 런이 끝나고 허브로 돌아가며, 허브의 포탈로 새 런을 시작한다.

| 클래스 | 책임 | 수명 |
|---|---|---|
| `UGJGameInstance` | `RebirthCount`(누적 도전 횟수), 레벨 전환 | 앱 전체 |
| `AGJGameMode` | 사망 감지, 딜레이, 게임오버 화면, 입력 전환 | 레벨마다 재생성 |
| `AGJRunPortal` | 허브에서 새 런 시작 (`IGJInteractable`) | 레벨 소속 |

흐름:
```
[전투 레벨] HP 0 → HandleDeath() → AGJGameMode::OnPlayerDied()
  → EndRun() (RebirthCount++) + DeathToGameOverDelay 타이머
  → ShowGameOverScreen() → 위젯 표시 + FInputModeUIOnly
  → "허브로" 클릭 → ReturnToHub() → OpenLevel(HubLevelName)

[허브] 포탈에 상호작용 → StartNewRun() → OpenLevel(CombatLevelName)
```

- **레벨 경로는 `Config/DefaultGame.ini`**의 `[/Script/Project_GJ.GJGameInstance]` 섹션에서 지정한다(`Config` 프로퍼티라 BP 서브클래스가 필요 없음).
- **회차 카운트는 사망 시점 한 곳에서만** 증가한다. 게임오버 위젯을 거치든, 위젯 미할당 폴백으로 바로 이동하든 중복되지 않는다.
- **인벤토리/무기는 명시적으로 초기화하지 않는다.** 레벨을 다시 여는 순간 캐릭터가 새로 스폰되어 자동 초기화된다.
- `RebirthCount`는 설계상 세이브에 저장되어 누적되는 값이지만, **저장은 아직 미구현**이라 앱을 종료하면 0으로 돌아간다(M4에서 해결).
```

- [ ] **Step 2: 9절 "알려진 갭 / TODO" 갱신**

기존 TODO 중 런 루프로 해결되는 항목은 없으므로 삭제할 줄은 없다. 다음 항목을 **추가**한다:
```markdown
- `RebirthCount`는 누적 도전 횟수로 설계되었으나 세이브가 없어 앱 종료 시 0으로 돌아감 (M4에서 해결 예정)
- 런은 사망으로만 끝남 — 클리어(승리) 조건은 아직 없음 (M5)
- 허브에는 포탈 하나뿐 — 상점/영구 강화는 미구현 (M6)
- `FItemData.bPersistAcrossRuns`는 여전히 읽히지 않음 — 회차 인계 규칙 미구현 (M3)
```

- [ ] **Step 3: `DevGuide.html`에 동일한 내용 반영**

`DevGuide.html`은 `Docs/DevGuide.md`와 같은 내용을 HTML로 옮긴 문서다. 위 두 단계에서 바꾼 내용을 동일하게 반영한다. 기존 문서의 CSS 클래스(`.path`, `.note`, `.meta`)와 표 구조를 그대로 따른다.

- [ ] **Step 4: 커밋**

```bash
git add Docs/DevGuide.md DevGuide.html
git commit -m "$(cat <<'EOF'
개발 가이드에 런 루프 구조 반영

M1에서 추가된 GJGameInstance 런 API, GJGameMode 사망 처리,
AGJRunPortal, UGJGameOverWidget과 회차 카운트 규칙을 문서화.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```
