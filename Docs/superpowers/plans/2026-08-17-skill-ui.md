# 스킬 상태 표시 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 어느 키에 무슨 스킬이 있고 쿨타임이 얼마나 남았는지 화면에서 보이게 하고, 차징 중 구체가 커지는 걸 보여준다.

**Architecture:** 컴포넌트가 `GetCooldownRatio`/`OnSkillSlotsChanged`/`SwapSkillSlots`를 노출하고, UI 위젯 둘(HUD 아이콘·인벤토리 칸)이 그걸 소비한다. 차징 구체는 `AGJCharacter`가 소유하는 `UStaticMeshComponent`를 스킬 컴포넌트가 조작한다.

**Tech Stack:** UE 5.8, C++ (UMG `BindWidget`/`BindWidgetOptional`, `UDragDropOperation`, `NativeTick`, `UStaticMeshComponent`)

**설계 문서:** `Docs/superpowers/specs/2026-08-17-skill-ui-design.md`

## Global Constraints

- **테스트 스위트가 없다.** 검증은 **Live Coding 컴파일 통과 + 수동 PIE 확인**이다.
- **새 `UPROPERTY`를 추가하면 라이브 코딩만으로는 부족하다 — 에디터 재시작이 필요하다.** M2.7에서 두 번 걸렸다(개발 가이드 10절). 이 계획은 Task 1·2·3·4가 각각 새 `UPROPERTY`를 추가하므로, **컴파일 후 에셋 작업 전에 재시작**을 기본으로 넣는다.
- **캐릭터는 스킬을 모른다.** 캐릭터에 추가하는 것은 구체 메시 컴포넌트와 접근자뿐이고, 켜고 끄고 키우는 것은 스킬 컴포넌트가 한다.
- **새 바인딩은 `BindWidgetOptional`.** strict로 두면 C++이 먼저 들어간 순간 기존 WBP 컴파일이 깨져 에디터 작업 전까지 게임이 안 돈다(`EXPBar`에서 정한 규칙). 단, **이번에 새로 만드는 WBP 안의 바인딩**은 위젯과 WBP가 같이 만들어지므로 strict로 둬도 된다.
- **인코딩**: 새 주석은 UTF-8 한글로 그냥 쓴다. 초기 파일의 깨진 옛 주석 줄은 건드리지 않는다.
- **커밋 메시지는 한국어**로 쓰고 본문 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`를 넣는다. 브랜치를 나누지 않고 `main`에서 작업한다.
- 에디터가 열려 있으면 UBT 빌드가 막힌다. 컴파일은 사용자에게 **Ctrl+Alt+F11**을 요청한다.
- PIE에서 `~` 콘솔은 디버그 매니저가 가로챈다. **Output Log 창의 `Cmd:` 입력칸**을 쓰고 명령은 **한 줄씩** 입력한다.
- **MCP(포트 8123)로 데이터 테이블·위젯·블루프린트를 만들 수 있다.** 실패하면 사용자에게 에디터 작업을 요청한다.

---

## 파일 구조

| 파일 | 책임 | 태스크 |
|---|---|---|
| `Source/Project_GJ/GJSkillComponent.h/.cpp` (수정) | 쿨타임 비율, 슬롯 변경 델리게이트, 슬롯 교환, 키 라벨, 구체 제어 | 1, 4 |
| `Source/Project_GJ/GJCardComponent.cpp` (수정) | 하드코딩된 키 이름을 `GetSlotKeyLabel`로 교체 | 1 |
| `Source/Project_GJ/GJCharacter.h/.cpp` (수정) | 콘솔 명령, 구체 메시 컴포넌트, HUD 초기화 호출 | 1, 2, 4 |
| `Source/Project_GJ/GJProjectile.h` (수정) | 메시 getter | 4 |
| `Source/Project_GJ/GJSkillIconWidget.h/.cpp` (신규) | HUD 아이콘 하나 | 2 |
| `Source/Project_GJ/GJPlayerHUDWidget.h/.cpp` (수정) | 아이콘 3개 바인딩 + 초기화 | 2 |
| `Source/Project_GJ/GJSkillSlotWidget.h/.cpp` (신규) | 인벤토리 칸 하나 (드래그 교환) | 3 |
| `Source/Project_GJ/GJInventoryWidget.h/.cpp` (수정) | 칸 3개 바인딩 + 갱신 | 3 |
| `Content/GJ/UI/WBP_SkillIcon`, `WBP_SkillSlot` (신규 에셋) | 위젯 | 2, 3 |
| `Content/GJ/UI/WBP_PlayerHUD`, `WBP_Inventory` (수정 에셋) | 배치 | 2, 3 |
| `Docs/DevGuide.md`, `DevGuide.html` (수정) | 문서화 | 5 |

**태스크 순서 근거**: API 토대(1, 콘솔로 검증) → HUD(2, 눈에 보임) → 인벤토리(3, 교환) → 구체(4, 월드 연출) → 문서(5). Task 1이 끝나면 UI 없이 콘솔로 교환과 쿨타임이 검증된다.

---

## Task 1: 컴포넌트 API와 키 라벨 통합

UI가 소비할 것을 먼저 만든다. **검증은 콘솔이다 — 위젯 없이 확인된다.**

**Files:**
- Modify: `Source/Project_GJ/GJSkillComponent.h`, `Source/Project_GJ/GJSkillComponent.cpp`
- Modify: `Source/Project_GJ/GJCardComponent.cpp`
- Modify: `Source/Project_GJ/GJCharacter.h`, `Source/Project_GJ/GJCharacter.cpp`

**Interfaces:**
- Produces:
  - `FOnSkillSlotsChangedSignature` (파라미터 없는 다이내믹 멀티캐스트)
  - `UGJSkillComponent::OnSkillSlotsChanged` (`BlueprintAssignable`)
  - `float UGJSkillComponent::GetCooldownRatio(int32 SlotIndex) const`
  - `void UGJSkillComponent::SwapSkillSlots(int32 SlotA, int32 SlotB)`
  - `static FText UGJSkillComponent::GetSlotKeyLabel(int32 SlotIndex)`
  - `AGJCharacter::GJSwapSkills(int32, int32)` (`UFUNCTION(Exec)`)

- [ ] **Step 1: `GJSkillComponent.h`에 델리게이트와 API 추가**

`GJSkillComponent.h`에서 다음 줄을 찾는다:

```cpp
// UPROPERTY TMap은 값으로 TArray를 직접 담지 못해서 한 겹 감싼다.
```

그 **위**에 추가한다:

```cpp
// 슬롯 내용(어느 칸에 무슨 스킬)이 바뀌었다. HUD와 인벤토리가 구독한다.
// 쿨타임은 여기 안 태운다 - 매 프레임 바뀌는 값이라 델리게이트로 밀면 방송만 하다 끝난다.
DECLARE_DYNAMIC_MULTICAST_DELEGATE(FOnSkillSlotsChangedSignature);

```

이어서 `public:` 블록의 `LogSkillInfo()` 선언 **아래**에 추가한다:

```cpp

    UPROPERTY(BlueprintAssignable, Category = "Skill")
    FOnSkillSlotsChangedSignature OnSkillSlotsChanged;

    // 0 = 준비됨, 1 = 방금 썼음. 빈 슬롯이나 쿨타임 0인 스킬도 0.
    // UI가 매 프레임 물어본다.
    UFUNCTION(BlueprintPure, Category = "Skill")
    float GetCooldownRatio(int32 SlotIndex) const;

    // 두 슬롯의 스킬과 쿨타임을 함께 맞바꾼다.
    UFUNCTION(BlueprintCallable, Category = "Skill")
    void SwapSkillSlots(int32 SlotA, int32 SlotB);

    // 슬롯 번호에 대응하는 키 이름. 지금까지 이 문자열이 여러 곳에 하드코딩돼 있었다.
    UFUNCTION(BlueprintPure, Category = "Skill")
    static FText GetSlotKeyLabel(int32 SlotIndex);
```

- [ ] **Step 2: `GJSkillComponent.cpp`에 구현 추가**

`GJSkillComponent.cpp`의 `LogSkillInfo` 함수 **위**에 추가한다:

```cpp
FText UGJSkillComponent::GetSlotKeyLabel(int32 SlotIndex)
{
    // IMC_GJ의 IA_Skill1/2/3 매핑과 일치해야 한다. 매핑을 바꾸면 여기도 바꾼다.
    switch (SlotIndex)
    {
    case 0:  return NSLOCTEXT("GJ", "SkillKey0", "우클릭");
    case 1:  return NSLOCTEXT("GJ", "SkillKey1", "Q");
    case 2:  return NSLOCTEXT("GJ", "SkillKey2", "F");
    default: return FText::GetEmpty();
    }
}

float UGJSkillComponent::GetCooldownRatio(int32 SlotIndex) const
{
    if (!CooldownEndTime.IsValidIndex(SlotIndex))
    {
        return 0.f;
    }

    const UWorld* World = GetWorld();
    if (!World)
    {
        return 0.f;
    }

    const FSkillData* Skill = FindSkill(GetSkillInSlot(SlotIndex));
    if (!Skill || Skill->Cooldown <= 0.f)
    {
        return 0.f;
    }

    const float Remaining = CooldownEndTime[SlotIndex] - World->GetTimeSeconds();
    return FMath::Clamp(Remaining / Skill->Cooldown, 0.f, 1.f);
}

void UGJSkillComponent::SwapSkillSlots(int32 SlotA, int32 SlotB)
{
    if (!EquippedSkills.IsValidIndex(SlotA) || !EquippedSkills.IsValidIndex(SlotB) || SlotA == SlotB)
    {
        return;
    }

    // 차징 중인 슬롯이 섞이면 손을 뗐을 때 의도하지 않은 스킬이 나간다.
    if (ChargingSlot == SlotA || ChargingSlot == SlotB)
    {
        CancelCharge();
    }

    EquippedSkills.Swap(SlotA, SlotB);

    // 쿨타임도 같이 옮긴다. 안 그러면 스킬을 쓰고 자리를 바꾸는 것이
    // 쿨타임 초기화 수단이 된다.
    CooldownEndTime.Swap(SlotA, SlotB);

    OnSkillSlotsChanged.Broadcast();
}

```

- [ ] **Step 3: `EquipSkillInSlot`에서 방송 추가**

`GJSkillComponent.cpp`의 `EquipSkillInSlot` 안에서 다음 줄을 찾는다:

```cpp
    UE_LOG(LogTemp, Log, TEXT("EquipSkillInSlot: 슬롯 %d <- %s"), SlotIndex, *SkillId.ToString());
```

그 **아래**에 추가한다:

```cpp

    OnSkillSlotsChanged.Broadcast();
```

- [ ] **Step 4: `LogSkillInfo`가 `GetSlotKeyLabel`을 쓰게 교체**

`GJSkillComponent.cpp`의 `LogSkillInfo` 안에서 다음 줄을 찾는다:

```cpp
    const TCHAR* SlotKeys[GJ_SKILL_SLOT_COUNT] = { TEXT("우클릭"), TEXT("Q"), TEXT("F") };
```

이 줄을 **삭제한다.**

이어서 같은 함수 안에서 다음 줄을 찾는다:

```cpp
            i, SlotKeys[i],
```

이를 다음으로 교체한다:

```cpp
            i, *GetSlotKeyLabel(i).ToString(),
```

- [ ] **Step 5: 카드 컴포넌트의 하드코딩 제거**

`GJCardComponent.cpp`의 `ApplyCard` 안에서 다음 줄을 찾는다:

```cpp
        const TCHAR* SlotKeys[GJ_SKILL_SLOT_COUNT] = { TEXT("우클릭"), TEXT("Q"), TEXT("F") };
```

이 줄을 **삭제한다.**

이어서 다음 두 줄을 찾는다:

```cpp
                NSLOCTEXT("GJ", "ReplaceSkillSlot", "{0} 자리를 버리고 교체한다"),
                FText::FromString(SlotKeys[SlotIndex]));
```

이를 다음으로 교체한다:

```cpp
                NSLOCTEXT("GJ", "ReplaceSkillSlot", "{0} 자리를 버리고 교체한다"),
                UGJSkillComponent::GetSlotKeyLabel(SlotIndex));
```

- [ ] **Step 6: 콘솔 명령 추가**

`GJCharacter.h`의 `GJSkillInfo()` 선언 **아래**에 추가한다:

```cpp

    // 예) GJSwapSkills 0 1 -> 우클릭과 Q의 스킬을 (쿨타임까지) 맞바꾼다
    UFUNCTION(Exec)
    void GJSwapSkills(int32 SlotA, int32 SlotB);
```

`GJCharacter.cpp`의 `AGJCharacter::GJSkillInfo()` 함수의 닫는 `}` **아래**에 추가한다:

```cpp

void AGJCharacter::GJSwapSkills(int32 SlotA, int32 SlotB)
{
    if (!SkillComponent)
    {
        UE_LOG(LogTemp, Warning, TEXT("GJSwapSkills: SkillComponent가 없습니다."));
        return;
    }

    SkillComponent->SwapSkillSlots(SlotA, SlotB);
    SkillComponent->LogSkillInfo();
}
```

- [ ] **Step 7: 컴파일**

사용자에게 요청한다:
> **Ctrl+Alt+F11**로 컴파일해줘.

Run: `grep -cE "error C" Saved/Logs/Project_GJ.log`
Expected: `0`

- [ ] **Step 8: 콘솔로 교환과 쿨타임 확인**

사용자에게 요청한다:
> `TestLev`에서 플레이하고 Output Log의 `Cmd:` 입력칸에 **한 줄씩** 쳐줘:
>
> ```
> GJEquipSkill Skill_Fireball 0
> ```
>
> 우클릭으로 **한 발 쏜 직후 곧바로**:
>
> ```
> GJSwapSkills 0 1
> ```
>
> 그리고 바로:
>
> ```
> GJSkillInfo
> ```

확인 항목:
- 슬롯 1(Q)에 `Skill_Fireball`이 있고 슬롯 0은 비어 있다
- **슬롯 1의 쿨타임 잔량이 0이 아니다** — 쿨타임이 스킬을 따라 옮겨졌다는 뜻
- 키 라벨이 `우클릭 / Q / F`로 나온다
- 그 직후 **Q를 눌러도 안 나가고**, 쿨타임이 끝나면 나간다

**쿨타임이 0으로 나오면 실패다.** 그건 교환이 쿨타임 초기화 수단이 된다는 뜻이다.

- [ ] **Step 9: 커밋**

```bash
git add Source/Project_GJ/GJSkillComponent.h Source/Project_GJ/GJSkillComponent.cpp Source/Project_GJ/GJCardComponent.cpp Source/Project_GJ/GJCharacter.h Source/Project_GJ/GJCharacter.cpp
git commit -m "$(cat <<'EOF'
스킬 컴포넌트에 UI용 API 추가

GetCooldownRatio, OnSkillSlotsChanged, SwapSkillSlots를 열었다. UI가
아직 없어서 콘솔 명령 GJSwapSkills로만 확인된다.

갱신 경로를 둘로 나눴다. 슬롯 내용은 델리게이트로 방송하고 쿨타임
비율은 물어보게 한다. 내용은 가끔 바뀌는데 쿨타임은 매 프레임 바뀌어서,
쿨타임까지 델리게이트에 태우면 방송만 하다 끝난다.

SwapSkillSlots는 스킬과 쿨타임을 함께 맞바꾼다. 쿨타임을 두고 스킬만
옮기면 스킬을 쓰고 자리를 바꾸는 것이 쿨타임 초기화 수단이 된다.
카드로 새 스킬을 받는 EquipSkillInSlot은 반대로 쿨타임을 0으로 두는데,
그건 없던 스킬이 새로 들어오는 것이라 물려받을 쿨타임이 없기 때문이다.

키 이름 문자열이 LogSkillInfo와 카드 교체 화면에 각각 하드코딩돼
있었다. UI 두 개를 더하면 네 곳이 되므로 GetSlotKeyLabel로 모았다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: HUD 스킬 아이콘

**Files:**
- Create: `Source/Project_GJ/GJSkillIconWidget.h`, `Source/Project_GJ/GJSkillIconWidget.cpp`
- Modify: `Source/Project_GJ/GJPlayerHUDWidget.h`, `Source/Project_GJ/GJPlayerHUDWidget.cpp`
- Modify: `Source/Project_GJ/GJCharacter.cpp`
- Create (에셋): `Content/GJ/UI/WBP_SkillIcon`
- Modify (에셋): `Content/GJ/UI/WBP_PlayerHUD`, `Content/GJ/DataTables/DT_SkillData`

**Interfaces:**
- Consumes: `GetCooldownRatio`, `OnSkillSlotsChanged`, `GetSlotKeyLabel` (Task 1)
- Produces:
  - `UGJSkillIconWidget::SetSlotData(int32, AGJCharacter*)`
  - `UGJPlayerHUDWidget::InitializeSkillIcons(AGJCharacter*)`

- [ ] **Step 1: `GJSkillIconWidget.h` 생성**

새 파일 `Source/Project_GJ/GJSkillIconWidget.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Blueprint/UserWidget.h"
#include "GJSkillIconWidget.generated.h"

class UImage;
class UTextBlock;
class UProgressBar;
class AGJCharacter;

// HUD의 스킬 아이콘 하나. 슬롯 번호를 기억하고 그 슬롯의 스킬과 쿨타임을 그린다.
// 디자이너에서 WBP_PlayerHUD에 3개 배치하고 각각 SetSlotData(0/1/2, 캐릭터)를 받는다.
UCLASS()
class PROJECT_GJ_API UGJSkillIconWidget : public UUserWidget
{
    GENERATED_BODY()

public:
    // 무기 슬롯 위젯과 같은 진입점. 슬롯을 기억하고 델리게이트를 구독한 뒤 즉시 한 번 그린다.
    UFUNCTION(BlueprintCallable, Category = "Skill")
    void SetSlotData(int32 InSlotIndex, AGJCharacter* InOwningCharacter);

protected:
    // 쿨타임은 매 프레임 바뀌므로 틱에서 폴링한다. 아이콘 3개짜리라 비용이 없고,
    // 컴포넌트를 틱시키지 않으므로 "틱 안 쓰기" 설계도 그대로다.
    virtual void NativeTick(const FGeometry& MyGeometry, float InDeltaTime) override;

    virtual void NativeDestruct() override;

    // 슬롯 내용이 바뀔 때만 호출된다(아이콘/키 라벨).
    UFUNCTION()
    void RefreshSkill();

    void SetCooldownRatio(float Ratio);

    // 이번 태스크에서 WBP를 같이 만드므로 아이콘만 strict로 둔다.
    UPROPERTY(meta = (BindWidget))
    UImage* IconImage;

    UPROPERTY(meta = (BindWidgetOptional))
    UTextBlock* KeyText;

    // 쿨타임 표시를 두 개로 나눠 받는다. ProgressBar는 SetPercent, 머티리얼 Image는
    // 스칼라 파라미터라 호출이 아예 다르다. 둘 다 Optional로 두고 붙어 있는 쪽만
    // 갱신하면, WBP에서 아래서 위로든 방사형이든 골라도 여기가 안 바뀐다.
    UPROPERTY(meta = (BindWidgetOptional))
    UProgressBar* CooldownBar;

    UPROPERTY(meta = (BindWidgetOptional))
    UImage* CooldownImage;

    // CooldownImage에 물린 머티리얼의 이 스칼라 파라미터에 0~1을 넣는다.
    UPROPERTY(EditDefaultsOnly, Category = "Skill")
    FName CooldownParamName = FName("Progress");

    UPROPERTY()
    UMaterialInstanceDynamic* CooldownMID;

    int32 SlotIndex = INDEX_NONE;

    UPROPERTY()
    AGJCharacter* OwningCharacter;
};
```

- [ ] **Step 2: `GJSkillIconWidget.cpp` 생성**

새 파일 `Source/Project_GJ/GJSkillIconWidget.cpp`:

```cpp
#include "GJSkillIconWidget.h"
#include "GJCharacter.h"
#include "GJSkillComponent.h"
#include "Components/Image.h"
#include "Components/TextBlock.h"
#include "Components/ProgressBar.h"
#include "Materials/MaterialInstanceDynamic.h"

void UGJSkillIconWidget::SetSlotData(int32 InSlotIndex, AGJCharacter* InOwningCharacter)
{
    // 재사용될 수 있으므로 이전 구독을 먼저 끊는다.
    if (OwningCharacter)
    {
        if (UGJSkillComponent* OldSkills = OwningCharacter->GetSkillComponent())
        {
            OldSkills->OnSkillSlotsChanged.RemoveDynamic(this, &UGJSkillIconWidget::RefreshSkill);
        }
    }

    SlotIndex = InSlotIndex;
    OwningCharacter = InOwningCharacter;

    if (OwningCharacter)
    {
        if (UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent())
        {
            Skills->OnSkillSlotsChanged.AddDynamic(this, &UGJSkillIconWidget::RefreshSkill);
        }
    }

    if (KeyText)
    {
        KeyText->SetText(UGJSkillComponent::GetSlotKeyLabel(SlotIndex));
    }

    // 방사형 머티리얼을 쓰는 경우에만 MID를 만든다. ProgressBar 방식이면 null로 남는다.
    if (CooldownImage && !CooldownMID)
    {
        CooldownMID = CooldownImage->GetDynamicMaterial();
    }

    RefreshSkill();
}

void UGJSkillIconWidget::NativeDestruct()
{
    // 위젯이 사라진 뒤에도 델리게이트가 남아 있으면 죽은 객체를 부른다.
    if (OwningCharacter)
    {
        if (UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent())
        {
            Skills->OnSkillSlotsChanged.RemoveDynamic(this, &UGJSkillIconWidget::RefreshSkill);
        }
    }

    Super::NativeDestruct();
}

void UGJSkillIconWidget::RefreshSkill()
{
    if (!IconImage || !OwningCharacter)
    {
        return;
    }

    UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent();
    const FSkillData* Skill = Skills ? Skills->FindSkill(Skills->GetSkillInSlot(SlotIndex)) : nullptr;

    // 빈 슬롯이면 아이콘만 숨긴다. 키 라벨은 남겨서 "이 자리가 비어 있다"를 보여준다.
    if (!Skill || !Skill->Icon)
    {
        IconImage->SetVisibility(ESlateVisibility::Hidden);
        return;
    }

    IconImage->SetBrushFromTexture(Skill->Icon);
    IconImage->SetVisibility(ESlateVisibility::Visible);
}

void UGJSkillIconWidget::SetCooldownRatio(float Ratio)
{
    if (CooldownBar)
    {
        CooldownBar->SetPercent(Ratio);
        // 쿨타임이 0이면 덮개를 아예 숨긴다. 0%짜리 바가 얇게 남아 보이는 걸 막는다.
        CooldownBar->SetVisibility(Ratio > 0.f ? ESlateVisibility::HitTestInvisible : ESlateVisibility::Hidden);
    }

    if (CooldownMID)
    {
        CooldownMID->SetScalarParameterValue(CooldownParamName, Ratio);
    }
}

void UGJSkillIconWidget::NativeTick(const FGeometry& MyGeometry, float InDeltaTime)
{
    Super::NativeTick(MyGeometry, InDeltaTime);

    if (!OwningCharacter)
    {
        return;
    }

    if (UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent())
    {
        SetCooldownRatio(Skills->GetCooldownRatio(SlotIndex));
    }
}
```

- [ ] **Step 3: `GJPlayerHUDWidget.h`에 아이콘 3개 추가**

`GJPlayerHUDWidget.h`에서 다음 줄을 찾는다:

```cpp
class UProgressBar;
class UTextBlock;
```

이를 다음으로 교체한다:

```cpp
class UProgressBar;
class UTextBlock;
class UGJSkillIconWidget;
class AGJCharacter;
```

이어서 다음 줄을 찾는다:

```cpp
    UFUNCTION(BlueprintCallable, Category = "HUD")
    void UpdateEXP(float CurrentEXP, float RequiredEXP, int32 Level);
```

그 **아래**에 추가한다:

```cpp

    // 스킬 아이콘 3개에 슬롯 번호와 캐릭터를 넘긴다. 캐릭터가 HUD를 만들 때 한 번 부른다.
    // 아이콘이 GetOwningPlayerPawn()을 직접 캐스팅하지 않는 이유: 위젯 생성 시점에 폰이
    // 아직 없을 수 있고, 그러면 조용히 아무것도 안 그리는 상태로 굳는다.
    UFUNCTION(BlueprintCallable, Category = "HUD")
    void InitializeSkillIcons(AGJCharacter* InCharacter);
```

이어서 다음 두 줄을 찾는다:

```cpp
    UPROPERTY(meta = (BindWidgetOptional))
    UTextBlock* LevelText;
```

그 **아래**에 추가한다:

```cpp

    // EXPBar와 같은 이유로 Optional이다 - strict로 두면 C++이 먼저 들어간 순간
    // WBP_PlayerHUD 컴파일이 깨져서 에디터 작업 전까지 게임이 안 돈다.
    UPROPERTY(meta = (BindWidgetOptional))
    UGJSkillIconWidget* SkillIcon1;

    UPROPERTY(meta = (BindWidgetOptional))
    UGJSkillIconWidget* SkillIcon2;

    UPROPERTY(meta = (BindWidgetOptional))
    UGJSkillIconWidget* SkillIcon3;
```

- [ ] **Step 4: `GJPlayerHUDWidget.cpp`에 초기화 구현 추가**

`GJPlayerHUDWidget.cpp` 상단 include 블록에 추가한다:

```cpp
#include "GJSkillIconWidget.h"
```

파일 **맨 끝**에 추가한다:

```cpp

void UGJPlayerHUDWidget::InitializeSkillIcons(AGJCharacter* InCharacter)
{
    if (SkillIcon1) { SkillIcon1->SetSlotData(0, InCharacter); }
    if (SkillIcon2) { SkillIcon2->SetSlotData(1, InCharacter); }
    if (SkillIcon3) { SkillIcon3->SetSlotData(2, InCharacter); }
}
```

- [ ] **Step 5: 캐릭터가 HUD를 만들 때 초기화 호출**

`GJCharacter.cpp`에서 다음 줄을 찾는다(117번째 줄 부근):

```cpp
                PlayerHUDWidgetInstance->AddToViewport();
```

그 **아래**에 추가한다:

```cpp
                PlayerHUDWidgetInstance->InitializeSkillIcons(this);
```

- [ ] **Step 6: 컴파일 후 에디터 재시작**

사용자에게 요청한다:
> **Ctrl+Alt+F11**로 컴파일하고, **컴파일이 끝나면 에디터를 껐다 켜줘.** 새 `UPROPERTY`(`SkillIcon1/2/3`)와 새 `UCLASS`(`UGJSkillIconWidget`)를 추가해서, 재시작하지 않으면 위젯 부모 클래스 목록에 안 뜨거나 바인딩이 런타임에 null이 된다(개발 가이드 10절).

Run: `grep -cE "error C" Saved/Logs/Project_GJ.log`
Expected: `0`

- [ ] **Step 7: 스킬 아이콘 텍스처 지정**

`DT_SkillData`의 `Skill_Fireball` 행에 아이콘을 넣는다. 전용 아트가 없으므로 프로젝트에 있는 텍스처를 자리표시자로 쓴다(카드 아이콘과 같은 방식).

MCP `DataTableTools.set_rows`:

```
data_table: /Game/GJ/DataTables/DT_SkillData.DT_SkillData
values:     {"Skill_Fireball":{"icon":"/Game/LevelPrototyping/Textures/T_GridChecker_A.T_GridChecker_A"}}
```

**MCP 주의**: 오브젝트 참조는 `{"refPath": ...}` 객체 형태로 넘기면 **조용히 무시된다.** 위처럼 평문 소프트 경로 문자열이어야 한다. 넣은 뒤 `get_rows`로 `icon`이 `None`이 아닌지 확인하고 `save_assets`한다.

- [ ] **Step 8: `WBP_SkillIcon` 생성**

MCP `UMGToolSet.CreateWidgetBlueprint`로 `/Game/GJ/UI`에 부모 클래스 `/Script/Project_GJ.GJSkillIconWidget`으로 `WBP_SkillIcon`을 만든다.

트리 구성(`UMGToolSet.AddWidget`):

```
RootOverlay (Overlay)                 <- 아이콘 위에 쿨타임 덮개를 겹치기 위해 Overlay
  IconImage    (Image)    64x64
  CooldownBar  (ProgressBar)          <- 아이콘 전체를 덮음
  KeyText      (TextBlock)            <- 우하단 정렬
```

프로퍼티 설정(`ObjectTools.set_properties`, 이름은 `list_properties`로 먼저 확인):
- `IconImage`: `brush.imageSize = {x:64, y:64}`
- `CooldownBar`: `barFillType = "BottomToTop"`, `percent = 0`, `fillColorAndOpacity`를 검정 60% 정도로 (덮개처럼 보이게)
- `KeyText`: `font.size = 12`
- `KeyText` 슬롯: `horizontalAlignment = "HAlign_Right"`, `verticalAlignment = "VAlign_Bottom"`

`CooldownImage`는 **만들지 않는다.** 방사형 머티리얼은 이번 범위 밖이고, 나중에 `CooldownBar`를 지우고 `CooldownImage`를 넣으면 C++ 변경 없이 교체된다.

`UMGToolSet.CompileWidgetBlueprint`로 컴파일해 **strict `BindWidget`(`IconImage`)이 통과하는지** 확인한다.

- [ ] **Step 9: `WBP_PlayerHUD`에 스킬 줄 추가**

MCP로 `WBP_PlayerHUD`의 `StatusBox`(VerticalBox) 안, `EXPBar` **아래**에 추가한다:

```
SkillRow (HorizontalBox)
  SkillIcon1  (WBP_SkillIcon)
  SkillIcon2  (WBP_SkillIcon)
  SkillIcon3  (WBP_SkillIcon)
```

`AddWidget`의 `widgetClass`에는 **`_C` 서픽스가 붙은 생성 클래스 경로**를 넘긴다: `/Game/GJ/UI/WBP_SkillIcon.WBP_SkillIcon_C`.

각 아이콘의 `HorizontalBoxSlot`에 `padding` 우측 6 정도를 준다. `SkillRow`의 `VerticalBoxSlot`에는 `padding` 상단 8을 준다.

컴파일 후 `AssetTools.save_assets`로 `WBP_SkillIcon`, `WBP_PlayerHUD`, `DT_SkillData`를 저장한다.

- [ ] **Step 10: HUD 확인**

사용자에게 요청한다:
> 플레이하고 Output Log의 `Cmd:` 칸에 쳐줘:
>
> ```
> GJEquipSkill Skill_Fireball 0
> ```
>
> 그다음 우클릭으로 한 발 쏴줘.

확인 항목:
- EXP 바 아래에 **칸 3개**가 보이고 각각 `우클릭` / `Q` / `F` 라벨이 있다
- 첫 칸에만 아이콘이 있고 나머지 둘은 아이콘 없이 라벨만 있다
- 발사 직후 **첫 칸이 덮개로 가득 찼다가 3초에 걸쳐 아래로 빠진다**
- 쿨타임이 끝나면 덮개가 사라진다
- 이어서 `GJEquipSkill Skill_Fireball 2`를 치면 **세 번째 칸에 아이콘이 즉시 나타난다**(델리게이트가 도는지)

- [ ] **Step 11: 커밋**

```bash
git add Source/Project_GJ/GJSkillIconWidget.h Source/Project_GJ/GJSkillIconWidget.cpp Source/Project_GJ/GJPlayerHUDWidget.h Source/Project_GJ/GJPlayerHUDWidget.cpp Source/Project_GJ/GJCharacter.cpp Content/GJ/UI/WBP_SkillIcon.uasset Content/GJ/UI/WBP_PlayerHUD.uasset Content/GJ/DataTables/DT_SkillData.uasset
git commit -m "$(cat <<'EOF'
HUD에 스킬 아이콘 3개와 쿨타임 표시 추가

EXP 바 아래에 우클릭/Q/F 순으로 칸 3개가 뜬다. 쿨타임 동안 덮개가
아래로 빠지면서 남은 시간을 보여준다.

쿨타임은 위젯 틱에서 GetCooldownRatio를 폴링한다. 아이콘 3개짜리라
비용이 없고, 컴포넌트를 틱시키지 않으므로 틱 안 쓰기 설계도 그대로다.
슬롯 내용은 반대로 델리게이트로 받는다.

쿨타임 표시를 ProgressBar와 Image 둘로 나눠 Optional로 받는다.
ProgressBar는 SetPercent, 머티리얼 Image는 스칼라 파라미터라 호출이
아예 달라서, 붙어 있는 쪽만 갱신하면 WBP에서 어느 방식을 골라도 C++이
안 바뀐다. 지금은 아래서 위로 차오르는 ProgressBar를 쓰고, 나중에
방사형 머티리얼로 올릴 때 WBP만 고치면 된다.

아이콘 위젯은 GetOwningPlayerPawn을 직접 캐스팅하지 않고 캐릭터를
넘겨받는다. 위젯 생성 시점에 폰이 아직 없으면 조용히 아무것도 안
그리는 상태로 굳기 때문이다.

빈 슬롯도 키 라벨은 남긴다. 아이콘만 숨겨서 그 자리가 비어 있다는 걸
보여준다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 인벤토리 스킬 페이지

**Files:**
- Create: `Source/Project_GJ/GJSkillSlotWidget.h`, `Source/Project_GJ/GJSkillSlotWidget.cpp`
- Modify: `Source/Project_GJ/GJInventoryWidget.h`, `Source/Project_GJ/GJInventoryWidget.cpp`
- Create (에셋): `Content/GJ/UI/WBP_SkillSlot`
- Modify (에셋): `Content/GJ/UI/WBP_Inventory`

**Interfaces:**
- Consumes: `SwapSkillSlots`, `OnSkillSlotsChanged`, `GetSlotKeyLabel` (Task 1)
- Produces:
  - `UGJSkillSlotDragOp` (`SourceSlotIndex`)
  - `UGJSkillSlotWidget::SetSlotData(int32, AGJCharacter*)`
  - `UGJInventoryWidget::RefreshSkillSlots()`

- [ ] **Step 1: `GJSkillSlotWidget.h` 생성**

새 파일 `Source/Project_GJ/GJSkillSlotWidget.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Blueprint/UserWidget.h"
#include "Blueprint/DragDropOperation.h"
#include "GJSkillSlotWidget.generated.h"

class UImage;
class UTextBlock;
class AGJCharacter;

// 드래그 중인 스킬 칸의 출발 인덱스를 담아 나른다 (GJWeaponSlotWidget과 같은 패턴)
UCLASS()
class PROJECT_GJ_API UGJSkillSlotDragOp : public UDragDropOperation
{
    GENERATED_BODY()

public:
    UPROPERTY(BlueprintReadWrite, Category = "Skill")
    int32 SourceSlotIndex = -1;
};

// 인벤토리 스킬 페이지의 칸 하나. 드래그해서 다른 칸에 놓으면 두 슬롯의 스킬이
// (쿨타임까지) 서로 바뀐다 - 즉 어느 키에 둘지를 바꾼다.
// 클릭에는 아무 동작도 없다: 무기와 달리 스킬 셋은 항상 세 개가 다 활성이라
// "장착"이라는 개념이 없다.
UCLASS()
class PROJECT_GJ_API UGJSkillSlotWidget : public UUserWidget
{
    GENERATED_BODY()

public:
    UFUNCTION(BlueprintCallable, Category = "Skill")
    void SetSlotData(int32 InSlotIndex, AGJCharacter* InOwningCharacter);

protected:
    virtual FReply NativeOnMouseButtonDown(const FGeometry& InGeometry, const FPointerEvent& InMouseEvent) override;
    virtual void NativeOnDragDetected(const FGeometry& InGeometry, const FPointerEvent& InMouseEvent, UDragDropOperation*& OutOperation) override;
    virtual bool NativeOnDrop(const FGeometry& InGeometry, const FDragDropEvent& InDragDropEvent, UDragDropOperation* InOperation) override;

    UPROPERTY(meta = (BindWidget))
    UImage* IconImage;

    UPROPERTY(meta = (BindWidgetOptional))
    UTextBlock* KeyText;

    int32 SlotIndex = -1;

    UPROPERTY()
    AGJCharacter* OwningCharacter;
};
```

- [ ] **Step 2: `GJSkillSlotWidget.cpp` 생성**

새 파일 `Source/Project_GJ/GJSkillSlotWidget.cpp`:

```cpp
#include "GJSkillSlotWidget.h"
#include "GJCharacter.h"
#include "GJSkillComponent.h"
#include "Components/Image.h"
#include "Components/TextBlock.h"

void UGJSkillSlotWidget::SetSlotData(int32 InSlotIndex, AGJCharacter* InOwningCharacter)
{
    SlotIndex = InSlotIndex;
    OwningCharacter = InOwningCharacter;

    if (KeyText)
    {
        KeyText->SetText(UGJSkillComponent::GetSlotKeyLabel(SlotIndex));
    }

    if (!IconImage || !OwningCharacter)
    {
        return;
    }

    UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent();
    const FSkillData* Skill = Skills ? Skills->FindSkill(Skills->GetSkillInSlot(SlotIndex)) : nullptr;

    if (!Skill || !Skill->Icon)
    {
        IconImage->SetVisibility(ESlateVisibility::Hidden);
        return;
    }

    IconImage->SetBrushFromTexture(Skill->Icon);
    IconImage->SetVisibility(ESlateVisibility::Visible);
}

FReply UGJSkillSlotWidget::NativeOnMouseButtonDown(const FGeometry& InGeometry, const FPointerEvent& InMouseEvent)
{
    // 빈 칸은 끌 게 없다.
    if (InMouseEvent.GetEffectingButton() != EKeys::LeftMouseButton || !OwningCharacter)
    {
        return FReply::Unhandled();
    }

    UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent();
    if (!Skills || Skills->GetSkillInSlot(SlotIndex).IsNone())
    {
        return FReply::Unhandled();
    }

    return FReply::Handled().DetectDrag(TakeWidget(), EKeys::LeftMouseButton);
}

void UGJSkillSlotWidget::NativeOnDragDetected(const FGeometry& InGeometry, const FPointerEvent& InMouseEvent, UDragDropOperation*& OutOperation)
{
    UGJSkillSlotDragOp* DragOp = NewObject<UGJSkillSlotDragOp>(this);
    DragOp->SourceSlotIndex = SlotIndex;
    OutOperation = DragOp;
}

bool UGJSkillSlotWidget::NativeOnDrop(const FGeometry& InGeometry, const FDragDropEvent& InDragDropEvent, UDragDropOperation* InOperation)
{
    UGJSkillSlotDragOp* DragOp = Cast<UGJSkillSlotDragOp>(InOperation);
    if (!DragOp || !OwningCharacter)
    {
        return false;
    }

    UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent();
    if (!Skills)
    {
        return false;
    }

    // 교환 결과는 OnSkillSlotsChanged로 방송되고, 인벤토리 위젯이 그걸 받아 칸을 다시 그린다.
    // 여기서 직접 SetSlotData를 부르면 출발 칸이 안 갱신된다.
    Skills->SwapSkillSlots(DragOp->SourceSlotIndex, SlotIndex);
    return true;
}
```

- [ ] **Step 3: `GJInventoryWidget.h`에 칸 3개 추가**

`GJInventoryWidget.h`에서 다음 줄을 찾는다:

```cpp
class UGJWeaponSlotWidget;
```

그 **아래**에 추가한다:

```cpp
class UGJSkillSlotWidget;
```

이어서 다음 줄을 찾는다:

```cpp
    // 무기 페이지(슬롯 0/1) 아이콘/활성 표시를 최신 상태로 갱신
    UFUNCTION()
    void RefreshWeaponSlots();
```

그 **아래**에 추가한다:

```cpp

    // 스킬 페이지(슬롯 0/1/2) 아이콘을 최신 상태로 갱신
    UFUNCTION()
    void RefreshSkillSlots();
```

이어서 다음 두 줄을 찾는다:

```cpp
    UPROPERTY(meta = (BindWidgetOptional))
    UGJWeaponSlotWidget* WeaponSlotWidget2;
```

그 **아래**에 추가한다:

```cpp

    // ==========================================
    // 3페이지: 스킬 페이지 (슬롯 0/1/2 = 우클릭/Q/F)
    // ==========================================
    UPROPERTY(meta = (BindWidgetOptional))
    UGJSkillSlotWidget* SkillSlotWidget1;

    UPROPERTY(meta = (BindWidgetOptional))
    UGJSkillSlotWidget* SkillSlotWidget2;

    UPROPERTY(meta = (BindWidgetOptional))
    UGJSkillSlotWidget* SkillSlotWidget3;
```

- [ ] **Step 4: `GJInventoryWidget.cpp`에 갱신 구현 추가**

`GJInventoryWidget.cpp` 상단 include 블록에 추가한다:

```cpp
#include "GJSkillSlotWidget.h"
#include "GJSkillComponent.h"
```

이어서 `InitializeInventory` 안에서 다음 줄을 찾는다:

```cpp
        OwningCharacter->OnWeaponSlotsChanged.AddDynamic(this, &UGJInventoryWidget::RefreshWeaponSlots);
```

그 **아래**에 추가한다:

```cpp

        if (UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent())
        {
            Skills->OnSkillSlotsChanged.RemoveDynamic(this, &UGJInventoryWidget::RefreshSkillSlots);
            Skills->OnSkillSlotsChanged.AddDynamic(this, &UGJInventoryWidget::RefreshSkillSlots);
        }
```

이어서 다음 줄을 찾는다:

```cpp
    RefreshWeaponSlots();
}
```

이를 다음으로 교체한다:

```cpp
    RefreshWeaponSlots();
    RefreshSkillSlots();
}
```

그리고 `RefreshWeaponSlots` 함수의 닫는 `}` **아래**에 추가한다:

```cpp

void UGJInventoryWidget::RefreshSkillSlots()
{
    if (SkillSlotWidget1) { SkillSlotWidget1->SetSlotData(0, OwningCharacter); }
    if (SkillSlotWidget2) { SkillSlotWidget2->SetSlotData(1, OwningCharacter); }
    if (SkillSlotWidget3) { SkillSlotWidget3->SetSlotData(2, OwningCharacter); }
}
```

마지막으로 `BeginDestroy` 안에서 다음 줄을 찾는다:

```cpp
        OwningCharacter->OnWeaponSlotsChanged.RemoveDynamic(this, &UGJInventoryWidget::RefreshWeaponSlots);
```

그 **아래**에 추가한다:

```cpp

        if (UGJSkillComponent* Skills = OwningCharacter->GetSkillComponent())
        {
            Skills->OnSkillSlotsChanged.RemoveDynamic(this, &UGJInventoryWidget::RefreshSkillSlots);
        }
```

- [ ] **Step 5: 컴파일 후 에디터 재시작**

사용자에게 요청한다:
> **Ctrl+Alt+F11**로 컴파일하고 **에디터를 껐다 켜줘.** 새 `UCLASS`와 새 `UPROPERTY`가 있어서 재시작해야 위젯 부모 클래스 목록에 뜨고 바인딩이 잡힌다.

Run: `grep -cE "error C" Saved/Logs/Project_GJ.log`
Expected: `0`

- [ ] **Step 6: `WBP_SkillSlot` 생성**

MCP로 `/Game/GJ/UI`에 부모 클래스 `/Script/Project_GJ.GJSkillSlotWidget`으로 `WBP_SkillSlot`을 만든다.

트리:

```
RootOverlay (Overlay)
  IconImage  (Image)     64x64
  KeyText    (TextBlock) 우하단
```

`WBP_SkillIcon`과 달리 쿨타임 표시가 없다 — 인벤토리는 정지 상태에서 보는 화면이라 쿨타임을 실시간으로 그릴 이유가 없다.

`CompileWidgetBlueprint`로 strict `BindWidget`(`IconImage`) 통과를 확인한다.

- [ ] **Step 7: `WBP_Inventory`에 스킬 페이지 배치**

`UMGToolSet.GetWidgets`로 `WBP_Inventory`(실제 에셋 이름은 `ListWidgetBlueprints`로 `/Game/GJ/UI`를 조회해 확인한다)의 현재 트리를 먼저 읽는다. 무기 페이지(`WeaponSlotWidget1/2`)가 어디에 놓여 있는지 보고 **그 옆/아래 같은 층에** 스킬 칸 3개를 가로로 추가한다:

```
SkillPageRow (HorizontalBox)
  SkillSlotWidget1  (WBP_SkillSlot)
  SkillSlotWidget2  (WBP_SkillSlot)
  SkillSlotWidget3  (WBP_SkillSlot)
```

`AddWidget`의 `widgetClass`에는 `/Game/GJ/UI/WBP_SkillSlot.WBP_SkillSlot_C`를 넘긴다.

**탭/스위처 배치는 하지 않는다.** 기존 무기 페이지도 "둘 다 이 위젯의 자식으로 두면 `InitializeInventory` 한 번으로 갱신된다"는 전제로 만들어져 있고(`GJInventoryWidget.h` 주석), 페이지 전환 UI는 원래 디자이너 몫이다. 지금은 보이기만 하면 검증이 된다.

컴파일 후 `WBP_SkillSlot`과 인벤토리 WBP를 저장한다.

- [ ] **Step 8: 드래그 교환 확인**

사용자에게 요청한다:
> 플레이하고 `Cmd:` 칸에 `GJEquipSkill Skill_Fireball 0`을 친 뒤 **우클릭으로 한 발 쏘고**, 바로 **Tab**을 눌러줘.
>
> 스킬 칸에서 **파이어볼을 Q 칸으로 드래그**하고 Tab으로 닫아줘.

확인 항목:
- 인벤토리에 스킬 칸 3개가 보이고 각각 `우클릭` / `Q` / `F` 라벨
- 첫 칸에만 아이콘
- 드래그하면 **자리가 바뀌고 인벤토리 화면도 즉시 갱신**된다(출발 칸이 비고 도착 칸에 아이콘)
- 닫으면 **HUD도 바뀌어 있다**(두 UI가 같은 델리게이트를 듣는지)
- **Q를 눌러도 아직 안 나간다** — 쿨타임이 스킬을 따라왔다는 뜻. 잠시 뒤엔 나간다

**Q가 즉시 나가면 실패다** — 교환이 쿨타임을 초기화했다는 뜻이다.

- [ ] **Step 9: 커밋**

```bash
git add Source/Project_GJ/GJSkillSlotWidget.h Source/Project_GJ/GJSkillSlotWidget.cpp Source/Project_GJ/GJInventoryWidget.h Source/Project_GJ/GJInventoryWidget.cpp Content/GJ/UI/WBP_SkillSlot.uasset Content/GJ/UI/WBP_Inventory.uasset
git commit -m "$(cat <<'EOF'
인벤토리에 스킬 페이지 추가

칸 3개를 드래그해서 서로 바꾸면 어느 키에 둘지가 바뀐다. 무기 페이지와
같은 패턴이라 드래그 오퍼레이션도 같은 모양이다.

클릭에는 아무 동작도 없다. 무기 칸은 클릭이 장착이지만 스킬은 세 개가
항상 활성이라 장착이라는 개념이 없다.

드롭 처리는 SetSlotData를 직접 부르지 않고 SwapSkillSlots만 부른다.
갱신은 OnSkillSlotsChanged 방송을 인벤토리 위젯이 받아서 하는데,
여기서 직접 그리면 도착 칸만 갱신되고 출발 칸이 남는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 차징 구체

**Files:**
- Modify: `Source/Project_GJ/GJProjectile.h`
- Modify: `Source/Project_GJ/GJCharacter.h`, `Source/Project_GJ/GJCharacter.cpp`
- Modify: `Source/Project_GJ/GJSkillComponent.h`, `Source/Project_GJ/GJSkillComponent.cpp`

**Interfaces:**
- Produces: `AGJProjectile::GetMeshComp()`, `AGJCharacter::GetChargeOrbMesh()`

- [ ] **Step 1: `AGJProjectile`에 메시 getter 추가**

`GJProjectile.h`에서 다음 줄을 찾는다:

```cpp
    // 현재 사용 중인 총알인지 확인
    bool IsActive() const { return bIsActive; }
```

그 **아래**에 추가한다:

```cpp

    // 차징 미리보기가 이 메시를 그대로 복사해 쓴다. 그래야 차징 중 보이는 구체와
    // 실제로 날아가는 구체가 자동으로 같아진다.
    FORCEINLINE UStaticMeshComponent* GetMeshComp() const { return MeshComp; }
```

- [ ] **Step 2: `AGJCharacter`에 구체 메시 컴포넌트 추가**

`GJCharacter.h`에서 다음 세 줄을 찾는다:

```cpp
    // 액티브 스킬 (슬롯/쿨타임/차징 전부 여기 - 캐릭터는 입력만 넘긴다)
    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Skill")
    UGJSkillComponent* SkillComponent;
```

그 **아래**에 추가한다:

```cpp

    // 차징 중 총구 앞에 보이는 구체. 캐릭터가 소유하는 이유는 UGJSkillComponent가
    // UActorComponent라 트랜스폼이 없어 자식 메시를 못 달기 때문이다.
    // 켜고 끄고 키우는 것은 전부 스킬 컴포넌트가 한다 - 캐릭터는 스킬을 모른다.
    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Skill")
    UStaticMeshComponent* ChargeOrbMesh;
```

이어서 `public:` 블록의 `GetSkillComponent()` 선언 **아래**에 추가한다:

```cpp

    UFUNCTION(BlueprintPure, Category = "Skill")
    UStaticMeshComponent* GetChargeOrbMesh() const { return ChargeOrbMesh; }
```

- [ ] **Step 3: `AGJCharacter` 생성자에서 구체 생성**

`GJCharacter.cpp`에서 다음 줄을 찾는다:

```cpp
    SkillComponent = CreateDefaultSubobject<UGJSkillComponent>(TEXT("SkillComponent"));
```

그 **아래**에 추가한다:

```cpp

    ChargeOrbMesh = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("ChargeOrbMesh"));
    ChargeOrbMesh->SetupAttachment(RootComponent);
    // 순수 연출이라 충돌도 그림자도 필요 없다.
    ChargeOrbMesh->SetCollisionEnabled(ECollisionEnabled::NoCollision);
    ChargeOrbMesh->SetCastShadow(false);
    ChargeOrbMesh->SetVisibility(false);
```

`GJCharacter.cpp` 상단 include 블록에 추가한다:

```cpp
#include "Components/StaticMeshComponent.h"
```

- [ ] **Step 4: 컴포넌트를 틱 가능하게 만들되 기본은 꺼둔다**

`GJSkillComponent.cpp`의 생성자에서 다음 두 줄을 찾는다:

```cpp
    // 차징과 쿨타임을 시각 비교로 계산하므로 매 프레임 할 일이 없다.
    PrimaryComponentTick.bCanEverTick = false;
```

이를 다음으로 교체한다:

```cpp
    // 차징과 쿨타임은 시각 비교로 계산하므로 평소엔 틱이 필요 없다.
    // 다만 차징 중에는 구체 크기를 매 프레임 갱신해야 해서 틱 자체는 가능하게 두고,
    // 시작할 때만 켠다. bCanEverTick이 false면 SetComponentTickEnabled가 아무 효과도 없다.
    PrimaryComponentTick.bCanEverTick = true;
    PrimaryComponentTick.bStartWithTickEnabled = false;
```

- [ ] **Step 5: `GJSkillComponent.h`에 구체 제어 선언 추가**

`GJSkillComponent.h`의 `protected:` 블록에서 다음 줄을 찾는다:

```cpp
    // 실제 발사. ChargeRatio는 0~1이다.
    void FireSkill(int32 SlotIndex, const FSkillData& Skill, float ChargeRatio);
```

그 **아래**에 추가한다:

```cpp

    virtual void TickComponent(float DeltaTime, ELevelTick TickType, FActorComponentTickFunction* ThisTickFunction) override;

    // 차징 시작 시 구체를 준비하고 보이게 한다. 메시를 못 찾으면 연출만 건너뛴다.
    void ShowChargeOrb(const FSkillData& Skill);

    // 구체를 숨기고 틱을 끈다. 발사·취소 어느 쪽으로 끝나든 반드시 지나간다.
    void HideChargeOrb();

    // 지금 차징 비율(0~1). 발사 계산과 구체 크기가 같은 값을 쓰게 한다.
    float GetChargeRatio() const;
```

- [ ] **Step 6: `GJSkillComponent.cpp`에 구체 제어 구현 추가**

`GJSkillComponent.cpp` 상단 include 블록에 추가한다:

```cpp
#include "Components/StaticMeshComponent.h"
```

파일 **맨 끝**에 추가한다:

```cpp

float UGJSkillComponent::GetChargeRatio() const
{
    if (ChargingSlot == INDEX_NONE)
    {
        return 0.f;
    }

    const UWorld* World = GetWorld();
    const FSkillData* Skill = FindSkill(GetSkillInSlot(ChargingSlot));
    if (!World || !Skill || Skill->ChargeTime <= 0.f)
    {
        return 0.f;
    }

    return FMath::Clamp((World->GetTimeSeconds() - ChargeStartTime) / Skill->ChargeTime, 0.f, 1.f);
}

void UGJSkillComponent::ShowChargeOrb(const FSkillData& Skill)
{
    AGJCharacter* Character = GetOwnerCharacter();
    if (!Character)
    {
        return;
    }

    UStaticMeshComponent* Orb = Character->GetChargeOrbMesh();
    if (!Orb)
    {
        return;
    }

    // 발사될 구체의 클래스에서 메시를 그대로 가져온다. 데이터를 따로 두면 언젠가
    // 차징 중 모습과 실제 발사체가 어긋나고, 고칠 때까지 아무도 모른다.
    TSubclassOf<AGJProjectile> ProjClass = Skill.ProjectileClass ? Skill.ProjectileClass : DefaultProjectileClass;
    if (!ProjClass)
    {
        return;
    }

    const AGJProjectile* ProjCDO = ProjClass->GetDefaultObject<AGJProjectile>();
    UStaticMeshComponent* SrcMesh = ProjCDO ? ProjCDO->GetMeshComp() : nullptr;
    if (!SrcMesh || !SrcMesh->GetStaticMesh())
    {
        // 연출이 게임플레이를 막으면 안 된다. 구체 없이 차징은 정상 진행된다.
        return;
    }

    Orb->SetStaticMesh(SrcMesh->GetStaticMesh());
    for (int32 i = 0; i < SrcMesh->GetNumMaterials(); i++)
    {
        Orb->SetMaterial(i, SrcMesh->GetMaterial(i));
    }

    // 발사체는 액터 스케일과 메시의 상대 스케일이 곱해져 보인다(BP_GJSkillProjectile은
    // 메시가 3배). 미리보기는 컴포넌트 하나뿐이라 그 상대 스케일을 직접 곱해야 크기가 맞는다.
    Orb->SetRelativeLocation(MuzzleOffset);
    Orb->SetRelativeScale3D(SrcMesh->GetRelativeScale3D() * Skill.BaseScale);
    Orb->SetVisibility(true);

    SetComponentTickEnabled(true);
}

void UGJSkillComponent::HideChargeOrb()
{
    SetComponentTickEnabled(false);

    if (AGJCharacter* Character = GetOwnerCharacter())
    {
        if (UStaticMeshComponent* Orb = Character->GetChargeOrbMesh())
        {
            Orb->SetVisibility(false);
        }
    }
}

void UGJSkillComponent::TickComponent(float DeltaTime, ELevelTick TickType, FActorComponentTickFunction* ThisTickFunction)
{
    Super::TickComponent(DeltaTime, TickType, ThisTickFunction);

    AGJCharacter* Character = GetOwnerCharacter();
    const FSkillData* Skill = (ChargingSlot != INDEX_NONE) ? FindSkill(GetSkillInSlot(ChargingSlot)) : nullptr;
    if (!Character || !Skill)
    {
        // 차징이 아닌데 틱이 돌고 있다. 정리하고 끈다.
        HideChargeOrb();
        return;
    }

    UStaticMeshComponent* Orb = Character->GetChargeOrbMesh();
    if (!Orb || !Orb->IsVisible())
    {
        return;
    }

    // 발사와 같은 공식이다. 그래야 미리보기 크기가 실제 발사 크기와 일치한다.
    const float Multiplier = 1.f + (Skill->MaxChargeMultiplier - 1.f) * GetChargeRatio();

    // 클래스가 비어 있으면 GetDefaultObject를 부를 수 없다. ShowChargeOrb가 이미
    // 그 경우를 걸러 구체를 안 띄우지만, 여기서도 막아야 크래시가 안 난다.
    TSubclassOf<AGJProjectile> ProjClass = Skill->ProjectileClass ? Skill->ProjectileClass : DefaultProjectileClass;
    if (!ProjClass)
    {
        return;
    }

    const AGJProjectile* ProjCDO = ProjClass->GetDefaultObject<AGJProjectile>();
    if (UStaticMeshComponent* SrcMesh = ProjCDO ? ProjCDO->GetMeshComp() : nullptr)
    {
        Orb->SetRelativeScale3D(SrcMesh->GetRelativeScale3D() * Skill->BaseScale * Multiplier);
    }
}
```

- [ ] **Step 7: 차징 시작/종료에 구체 연결**

`GJSkillComponent.cpp`의 `OnSkillPressed` 안에서 다음 두 줄을 찾는다:

```cpp
    ChargingSlot = SlotIndex;
    ChargeStartTime = World->GetTimeSeconds();
```

그 **아래**에 추가한다:

```cpp
    ShowChargeOrb(*Skill);
```

이어서 `CancelCharge` 함수를 찾아 다음으로 교체한다:

```cpp
void UGJSkillComponent::CancelCharge()
{
    ChargingSlot = INDEX_NONE;
    ChargeStartTime = 0.f;

    // 회피·인벤토리·카드 화면·사망이 전부 이 함수를 지나간다. 여기서 끄면 경로마다
    // 따로 챙길 필요가 없다.
    HideChargeOrb();
}
```

마지막으로 `OnSkillReleased` 안에서 다음 줄을 찾는다:

```cpp
    const float StartTime = ChargeStartTime;
    CancelCharge();
```

이 두 줄은 **그대로 둔다** — `CancelCharge`가 이미 구체를 숨기므로 발사 경로에서도 자동으로 정리된다.

- [ ] **Step 8: 컴파일 후 에디터 재시작**

사용자에게 요청한다:
> **Ctrl+Alt+F11**로 컴파일하고 **에디터를 껐다 켜줘.** `AGJCharacter`에 새 `UPROPERTY`(`ChargeOrbMesh`)를 추가해서, 재시작하지 않으면 런타임에 null이 될 수 있다.

Run: `grep -cE "error C" Saved/Logs/Project_GJ.log`
Expected: `0`

- [ ] **Step 9: 구체 확인**

사용자에게 요청한다:
> 플레이하고 `GJEquipSkill Skill_Fireball 0`을 친 뒤 **우클릭을 천천히 꾹 눌러봐.**

확인 항목:
- 캐릭터 앞쪽 위에 **구체가 나타나 점점 커진다**
- 뗐을 때 **날아가는 구체 크기가 방금 보던 크기와 같다**
- **회피(Shift)로 취소하면 구체가 사라진다**
- Tab으로 인벤토리를 열어도 구체가 사라진다
- 차징 중 레벨업 → 카드 화면이 뜨고 구체가 사라진다
- 차징을 안 할 때는 구체가 안 보인다

- [ ] **Step 10: 커밋**

```bash
git add Source/Project_GJ/GJProjectile.h Source/Project_GJ/GJCharacter.h Source/Project_GJ/GJCharacter.cpp Source/Project_GJ/GJSkillComponent.h Source/Project_GJ/GJSkillComponent.cpp
git commit -m "$(cat <<'EOF'
차징 중 구체 미리보기 추가

우클릭을 누르고 있으면 캐릭터 앞에 구체가 나타나 발사될 크기까지
점점 커진다.

메시는 FSkillData가 아니라 ProjectileClass CDO에서 읽는다. 차징 중
보이는 구체와 실제로 날아가는 구체가 자동으로 같아진다. 데이터를 따로
두면 언젠가 어긋나고 고칠 때까지 아무도 모른다.

크기 계산은 발사와 같은 공식을 쓰고, 발사체의 메시 상대 스케일까지
곱한다. 발사체는 액터 스케일과 메시 상대 스케일이 곱해져 보이는데
미리보기는 컴포넌트 하나뿐이라 그걸 직접 곱해야 크기가 맞는다.

구체 메시는 캐릭터가 소유한다. UGJSkillComponent가 UActorComponent라
트랜스폼이 없어 자식 메시를 못 단다. 켜고 끄고 키우는 것은 전부 스킬
컴포넌트가 하므로 캐릭터는 여전히 스킬을 모른다.

틱은 차징 중에만 켠다. bCanEverTick을 true로 바꾸되
bStartWithTickEnabled를 false로 둔다 - bCanEverTick이 false면
SetComponentTickEnabled가 아무 효과도 없다.

구체를 숨기는 것은 CancelCharge 한 곳에서만 한다. 회피, 인벤토리,
카드 화면, 사망, 발사가 전부 그 함수를 지나가므로 경로마다 따로
챙길 필요가 없다.

메시를 못 찾으면 연출만 건너뛰고 차징과 발사는 정상 동작한다.
시각 요소가 게임플레이를 막으면 안 된다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 개발 가이드 갱신

**Files:**
- Modify: `Docs/DevGuide.md`
- Modify: `DevGuide.html`

- [ ] **Step 1: 6.8절에 UI 내용 추가**

`Docs/DevGuide.md`의 6.8절 끝(콘솔 명령 문단 앞)에 다음을 담은 소절을 추가한다:

- **갱신 경로가 둘로 나뉜다**: 슬롯 내용은 `OnSkillSlotsChanged` 델리게이트, 쿨타임 비율은 위젯 `NativeTick`에서 `GetCooldownRatio` 폴링. 쿨타임까지 델리게이트에 태우면 방송만 하다 끝난다
- **위젯 틱은 컴포넌트의 "틱 안 쓰기"와 충돌하지 않는다** — 컴포넌트는 여전히 시각 비교만 하고, 물어보는 쪽이 UI일 뿐이다
- **`SwapSkillSlots`는 쿨타임도 함께 옮긴다** — 안 그러면 교환이 쿨타임 초기화 수단이 된다. 반대로 `EquipSkillInSlot`(카드로 새 스킬 획득)은 쿨타임을 0으로 둔다. 둘은 다른 동작이라 다른 함수다
- **`GetSlotKeyLabel`이 키 이름의 단일 출처**다. `IMC_GJ` 매핑을 바꾸면 여기도 바꾼다
- **차징 구체 메시는 `ProjectileClass` CDO에서 읽는다** — 미리보기와 실제 발사체가 자동으로 같아진다. 크기는 발사와 같은 공식 + 발사체 메시의 상대 스케일까지 곱한다
- **틱은 차징 중에만 켠다** — `bCanEverTick = true` + `bStartWithTickEnabled = false`. `bCanEverTick`이 false면 `SetComponentTickEnabled`가 아무 효과도 없다
- **구체를 숨기는 곳은 `CancelCharge` 하나뿐**이다. 회피·인벤토리·카드 화면·사망·발사가 전부 거기를 지나간다

- [ ] **Step 2: 7절 위젯 표에 3개 추가**

`Docs/DevGuide.md` 7절의 위젯 표에서 `UGJCardWidget` 행을 찾아 그 **아래**에 추가한다:

```markdown
| `UGJSkillIconWidget` | `WBP_SkillIcon` | `IconImage` (strict) / `KeyText`, `CooldownBar`, `CooldownImage` (**Optional**) | `SetSlotData(Slot, Character)` | `WBP_PlayerHUD`의 `SkillRow`에 3개 |
| `UGJSkillSlotWidget` | `WBP_SkillSlot` | `IconImage` (strict) / `KeyText` (**Optional**) | `SetSlotData(Slot, Character)` → 드래그 시 `SwapSkillSlots` | 인벤토리 스킬 페이지에 3개 |
```

그리고 `UGJPlayerHUDWidget` 행의 BindWidget 칸에 `SkillIcon1/2/3 (Optional)`을, 갱신 함수 칸에 `InitializeSkillIcons(Character)`를 더한다.

**`CooldownBar`와 `CooldownImage`를 둘 다 Optional로 받는 이유**를 표 아래 문단으로 적는다: `ProgressBar`는 `SetPercent`, 머티리얼 `Image`는 스칼라 파라미터라 호출이 아예 다르다. 붙어 있는 쪽만 갱신하면 WBP에서 아래서 위로든 방사형이든 골라도 C++이 안 바뀐다. 지금은 `ProgressBar`(`BarFillType = BottomToTop`)를 쓴다.

- [ ] **Step 3: 9절 TODO 갱신**

다음 두 항목을 **삭제한다**:

```markdown
- **스킬 쿨타임·슬롯 HUD가 없음** — 어느 키에 무슨 스킬이 있는지 `GJSkillInfo` 콘솔로만 확인 가능. 슬롯 교체가 가능해진 만큼 실제로 불편하다
- 차징 중 시각 피드백이 없음 — 얼마나 찼는지 화면에 안 보이고, 시전 애니메이션·발사 이펙트도 없다. 구체가 그냥 나타난다
```

같은 목록 끝에 다음을 **추가한다**:

```markdown
- 쿨타임 표시가 시계방향 차오름이 아니라 아래서 위로 차오르는 형태다 — 방사형 마스크 머티리얼을 만들면 `WBP_SkillIcon`에서 `CooldownBar`를 `CooldownImage`로 바꾸는 것만으로 교체된다(C++ 변경 없음)
- 스킬 아이콘이 자리표시자 텍스처다 (`T_GridChecker_A`)
- MP가 부족할 때 아이콘에 표시가 없다 — 눌러도 안 나가는 이유를 화면에서 알 수 없다
- 차징 구체에 이펙트·머티리얼 연출이 없다. 발사될 구체의 메시를 그대로 키울 뿐이고 시전 애니메이션도 없다
- 인벤토리 스킬 칸에 마우스를 올려도 설명 툴팁이 없다
- 인벤토리의 페이지 전환(가방/무기/스킬) UI가 없다 — 세 페이지가 같은 위젯의 자식으로 나란히 있을 뿐이다
```

- [ ] **Step 4: `DevGuide.html`에 동일 내용 반영**

Step 1~3의 변경을 동일하게 반영한다. 기존 문서의 CSS 클래스(`.note`, `.meta`, `.path`)와 표 구조를 그대로 따른다.

반영 후 태그 균형을 확인한다:

Run:
```bash
python -c "
import re
html = open('DevGuide.html', encoding='utf-8').read()
bad = 0
for tag in ['table','ul','pre','div','p','li','tr','td','th','code','h2','h3','b','span']:
    o = len(re.findall(r'<'+tag+r'[ >]', html)); c = len(re.findall(r'</'+tag+r'>', html))
    if o != c:
        bad += 1
        print(f'{tag}: {o} vs {c}  MISMATCH')
print('OK' if bad==0 else f'{bad} MISMATCH')
"
```
Expected: `OK`

- [ ] **Step 5: 커밋**

```bash
git add Docs/DevGuide.md DevGuide.html
git commit -m "$(cat <<'EOF'
개발 가이드에 스킬 UI 반영

6.8절에 갱신 경로가 둘로 나뉘는 이유(내용은 델리게이트, 쿨타임은 위젯
틱 폴링)와 위젯 틱이 컴포넌트의 틱 안 쓰기 결정과 충돌하지 않는 이유를
적었다.

SwapSkillSlots가 쿨타임도 함께 옮기는 이유를 남겼다. 안 그러면 교환이
쿨타임 초기화 수단이 된다.

차징 구체가 ProjectileClass CDO에서 메시를 읽는 이유와, 크기 계산에
발사체 메시의 상대 스케일까지 곱해야 하는 이유를 적었다.

틱을 차징 중에만 켜는 방법도 남겼다. bCanEverTick이 false면
SetComponentTickEnabled가 아무 효과도 없다.

7절 위젯 표에 아이콘/슬롯 위젯을 넣고, 쿨타임 표시를 두 타입으로 나눠
Optional로 받는 이유를 적었다.

9절에서 "스킬 HUD 없음"과 "차징 시각 피드백 없음"을 지우고 남은 갭을
적었다. 방사형 머티리얼은 WBP만 고치면 교체된다는 것도 함께.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```
